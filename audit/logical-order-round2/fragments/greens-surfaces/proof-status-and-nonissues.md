# Proof boundaries and checked nonissues: Chapters 14–15

All16 sections, both chapter wrappers, all79 tagged exercises and all93 root task candidates were semantically read. The task ledger additionally records three skill-list directives and four Green-checklist table rows omitted by the imperative scanner, for100 rows. Every tagged task has an individual outline, an exact available support route and a verdict. Figure code, captions and visible mathematical labels were read; no rendered page, clipping, interactivity or image visibility was certified by this worker.

## Green's theorem

- `c14s2-thm-green-circulation:86–104` and `c14s3-thm-green-flux-form:257–275` accurately require a bounded closed region, finitely many disjoint piecewise smooth simple boundary curves, positive orientation, and C1 coefficients on an open neighborhood. The flux form follows by `P=-N,Q=M`; the normal formula for a positive boundary is `(dy/ds,-dx/ds)`. The comparison checkpoint follows the two statements and needs only that substitution. Old M22 is resolved.
- `c14s4-def-exterior-derivative-plane-1-form:131–142` locally defines the coordinate operation. The expansion at154–172 uses scalar differentials and bilinear alternating algebra; it does not borrow the future general exterior-product derivative rule. Earlier13.8 already supplies this plane special case. The later systematic forms chapters are not a hidden prerequisite.
- `c14s5-subsec-proof-rectangle:92–143` is an exact proof by opposite-side FTC and finite Fubini. The finite rectangular-union extension uses only cancellation and additivity. The TypeI formula at374–394 has the correct moving-endpoint derivative. Its supporting parameter-integral lemma is stated earlier, while ROOT03 records the missing finite-cover support inside that lemma's claimed proof. This inherited support issue does not invalidate the algebraic boundary calculation.
- `c14s5-proof-scope:406–414` explicitly distinguishes finite simple decompositions from arbitrary piecewise smooth curves with infinitely many oscillations. The staircase limit discussion is expressly a sketch, not a full general Green proof. The general theorem is supplied honestly and may be used. Old M21's claim of an entirely closed supporting proof is qualified by ROOT03, but the newly written finite/general distinction itself is correct.
- Truncating ellipse/disc endpoint charts at396–404 is legitimate: omitted boundary lengths and areas tend to zero, while a continuous field on the closed region is bounded by earlier EVT. This is not a claim that all piecewise smooth regions have finite monotone decomposition.
- All annulus examples keep inner boundaries clockwise; angle-form and radial-field computations make the missing-point hypothesis failure explicit. The warning that an infinite or fractal boundary is outside the theorem is not a request to prove an advanced convergence theorem. Old M23's simple-curve and boundary1-form corrections are intact.

## Surfaces and classical integral theorems

- `c15s1-def-smooth-regular-surface-patch:267–287` says smooth means C1 and regular means independent surface partials. Its local graph explanation uses a nonzero2x2minor and the previously supplied inverse theorem. `c15s1-thm-tangent-plane-to-a-parametrized-surface:331–373` proves the plane using the full vector `o(sqrt(h²+k²))` remainder available in7.5. It is not inferred merely from two coordinate curves. This respects the author's initial ordinary grid-velocity lesson in5.2.
- The cone/sphere contrast is mathematically decisive: at the cone apex three generator directions are linearly independent and cannot all belong to a plane; at the sphere pole a different regular graph chart gives the plane. A vanishing cross product is only a defect of that parametrization until the geometric argument is given. The moved legacy exercise IDs remain attached to the appropriate tangent/orientation tasks.
- `c15s2-def-scalar-surface-integral:153–167` defines scalar integration for a regular once-covering patch and an integrable pulled-back density. Surface area is its nonnegative norm factor; the preceding infinitesimal-parallelogram discussion motivates that definition. This audit does not promote the motivation into an independently completed measure-theoretic area proof or global reparametrization-invariance theorem. No task in these sections asks students to prove those omitted general results.
- `c15s3-thm-flux-parametrized-surface:264–290` follows algebraically from the scalar surface-integral definition and the chosen unit normal; the proof's tiny-patch language is motivational but the exact product `n_hat ||r_u cross r_v||=r_u cross r_v` appears immediately before it. Once-coverage and reversal are explicitly stated.
- `c15s4-pullback-to-parameter-domain:217–278` carefully distinguishes source/target covectors and evaluates all three wedge shadows. `c15s4-def-surface-integral-2-form:280–300` states integrability, interior regularity, once-coverage and matching orientation. Later abbreviated equalities are expressly declared pullbacks at314–321.
- `c15s5-thm-stokes:303–378` states the compact oriented piecewise smooth theorem with C1 field on an open neighborhood. The written proof applies Green to a C2 regular patch and computes `B_u-A_v` using chain/product rules and equality of continuous mixed partials. It proves precisely the required naturality identity without assuming general naturality or generalized Stokes. Finite-patch cancellation then follows. The broader piecewise smooth case is explicitly accepted and the omitted approximation is acknowledged.
- `c15s6-thm-divergence-theorem:333–354` uses closure explicitly and a C1 field neighborhood. The box/curved-boundary distinction is honest: exact opposite-face FTC proves boxes, while the general curved statement is supplied. The local definition `dPhi=(P_x+Q_y+R_z)dx wedge dy wedge dz` is given before use, and its cyclic signs are checked by two swaps.
- `c15s7-thm-curl-of-gradient:67–89` and `c15s7-thm-divergence-of-curl:182–212` have C2 hypotheses and direct mixed-partial proofs. The counterexamples compute raw iterated partials; they do not claim `d²` acts on a coefficient class excluded from its definition. The checkpoint preceding the first theorem is independently solvable by the already proved work FTC, not by a future curl-gradient assertion.
- The local/global potential discussion accurately distinguishes a missing axis (circulation obstruction) from a missing point (flux obstruction). The radial field cannot be a global curl on punctured space because Stokes on the sphere would give zero flux; it does not need the field inside the sphere, only on a neighborhood of that sphere. This is a legitimate use of the supplied Stokes theorem. Its general local-potential existence route inherits ROOT03 when that deferred proof is considered, but the explicit obstruction computations do not.

## High-risk tasks solved independently

- **Drone sampling14.7:** equally spaced time samples are explicit at274–278. If `|x'|<=Mx, |y'|<=My`, one subarc's `integral x dy` differs from its chord's trapezoid by at most `2 Mx My h²`. Summing gives an error bound `2 Mx My T max(h)`, so area estimates converge. Bounded continuous derivatives use supplied EVT; no finite-cover theorem or unannounced numerical method is necessary. “Improves” is convergence language, not guaranteed monotonic improvement of every refinement. The noncircular example gives exact area6pi.
- **Screen15.9:** graph flux integrand is `-g_x-g_y/2+2-g/4`. Derivative terms integrate to zero and `integral g=1+4/(5pi²)`, so flux is `7/4-1/(5pi²)`. The exact branch of “evaluate exactly or estimate numerically” is available from entry calculus and finite Fubini.
- **Cone area15.9:** use `(s cos theta,s sin theta,s)`, area density `sqrt(2)s`, area `9pi sqrt(2)`. The apex is on the parameter boundary; omitting `s<epsilon` loses area `pi sqrt(2)epsilon²`, tending to zero. No tangent plane at the apex is assumed.
- **Weighted paraboloid15.9:** scalar integral is `2pi integral_0^1 (4-r²)sqrt(1+4r²)r dr=pi(175sqrt(5)-41)/60`. Using the flux formula would be wrong; the witness explicitly uses the norm area factor.
- **Plane/triangle Stokes15.9:** curl `(z,x,y)` is `(1,1,1)`, upward graph area vector is `(1,1,1)`, so flux is3/2. The compatible boundary vertices `(0,0,1),(1,0,0),(0,1,0)` give three work integrals1/2, independently checking the orientation and value.
- **Gauss project15.9:** field and physical constants are supplied; spherical area cancels the inverse-square factor and gives `4pi kq=q/epsilon0`. The shell's inner outward normal is radially inward. No charge-conservation PDE or electromagnetism theory is required to solve these five tasks.

## Limitations and source-first disposition

GS-01 is a residual statement-precision finding; the correct earlier Green theorem remains sufficient for the applications. The no-new-finding decisions above are based on actual local definitions, precise supplied-theorem status, or explicit computations, not on “standard knowledge.” Previous report claims of full coverage were not used as proof. No edits to textbook or old reports occurred. Root owns baseline structural validation and global graph closure. Independent challenge follows this primary pass; graphics review remains source-only unless a later receipt explicitly adds rendered evidence.
