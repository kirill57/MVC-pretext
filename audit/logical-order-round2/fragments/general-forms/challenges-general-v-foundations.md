# Independent challenge: general-forms worker versus foundations pass

Snapshot `9a9d110af839b539fe598de66ecc4320a4126af4`. This challenge followed completion of the general-forms primary source pass. I read the foundations findings/protected-judgments drafts, then independently opened the source passages and attempted to defeat their classifications using context, earlier statements, counterexamples, and exact scope checks. This is a targeted challenge of the reported findings and important nonissues, not a second claim that I independently reread all56 foundations sections or all442 formal tasks.

## Findings challenged

| Primary claim | Attempt to refute | Independent source/math check | Outcome |
|---|---|---|---|
| FND-01, continuity review needs accumulation-point qualifier | Perhaps every point under discussion is implicitly a domain accumulation point, or the book defines continuity by the three-condition test everywhere. | `sec-6-continuity.xml`60–76 expressly covers `F:D subset Rn` and isolated points;72 gives the unpunctured epsilon-delta condition, while78 restricts the three-condition list to accumulation points. `c6s6-ex-cc-three-conditions`126–133 drops that restriction. On D={a},f(a)=7, every epsilon admits every delta but the book's punctured limit convention does not define a limit at a. | Finding survives. P2 is defensible as an assessment quantifier mismatch, not a missing topology chapter or incorrect relative continuity definition. A charitable general answer could restate epsilon-delta continuity, but it would evade the clearly intended three listed conditions. |
| FND-02, basis images without linearity | Perhaps the exercise inherits the explicit linear T from the subsection, so “nothing else” means no other evaluated values rather than no hypothesis. | `sec-3-linear-transformations-r-n-r-m.xml`175–213 states T linear and proves the basis formula.217–237 immediately supplies I(x,y)=(x,y),N(x,y)=(x²,y), agreeing on e1,e2 but differing at(2,0).239 reiterates linearity. Checkpoint245–248 nevertheless says only the outputs “and nothing else about T,” while its hint253–255 invokes linearity. | Literal contradiction survives; contextual refutation substantially limits its instructional harm. Retain only P3 wording improvement. It is not an unavailable capability or grounds for moving the lesson. If author reads T as inheriting the standing hypothesis, this may be treated as optional clarification rather than defect count. |
| FND-03, triangle equality omits zero | Perhaps “same way” conventionally includes zero, or the hiker context requires distinct stops. | `sec-1-norm-distance-and-spheres.xml`336–343 explicitly separates “either vector is zero” from “both nonzero vectors point in the same direction.” Checkpoint350–353 uses “exactly when” without either restriction. No distinct-stop assumption is stated. u=0,v=(1,0) gives equality1=1; the zero input has no directional unit vector. | Finding survives at P3; preserve the correct immediately prior statement and later proof. Restricting to nonzero legs or restoring zero exception is enough. |

## Protected judgments challenged

| Protected judgment | Attempted failure mode and actual check | Outcome |
|---|---|---|
| Preserve author-directed5.2 frozen-parameter lesson | I tried to find a use of two-variable partial notation or an inference of a true geometric cone singularity merely from a zero chart cross product. Actual607–625 freezes one input and defines ordinary vector derivatives;642–663 distinguishes parameter regularity and explicitly points to15.1 for the full tangent-sheet claim. Lines691–695 use three cone-generator velocities with determinant2, a stronger obstruction.744–751 gives q(s,t)=(s³,t,0), whose cross product vanishes on s=0 although its image is a regular plane. The bowl checkpoint753–765 only needs the earlier one-variable vector derivative and cross product. | Protected. Normal(-2,2,1) at(1,-1) is correct; swapping velocities negates it. No obsolete prohibition should be restored. The later proof destination remains root's Ch15 responsibility. |
| Arbitrary-n determinant product is independent | I tried to trace a dependency from determinant multiplicativity to an area/volume scaling theorem that itself uses multiplicativity. The source at567–590 defines permutation signs, determinant, multilinearity and alternation algebraically.603–617 expands F(v1,...,vn)=det[Bv1...Bvn] in coordinate inputs, kills repeated indices and recovers det B times the permutation formula.634–649 then proves invertibility using the earlier basis-exchange fact. | No cycle found. The motivating signed-area paragraph558–564 is not the actual proof. Dimensional extension is explicit; no higher-dimensional volume theorem is needed. |
| Gradient formula before Chapter8 chain rule | I tried to identify a covert composition derivative in7.2. Source226–278 instead uses exact coordinate increments and two one-variable MVT applications. The evaluation points approach(a,b); continuity of both partials at that point gives the limit. If an increment is zero, its bracket is zero and any intermediate parameter works. | Protected, under exactly the stated existence-nearby/continuity-at-point hypotheses. Does not require the later matrix chain rule. |
| Continuous partials imply differentiability without compactness | I tested whether7.6 promotes a directional estimate to a uniform one without justification. Source36–90 bounds the remainder/norm by the sum of two partial-derivative differences. Both evaluated points lie within the shrinking radius r, so the epsilon-delta continuity at the base point supplies one bound for all increments. | Protected. This is pointwise continuity at one base point controlling nearby evaluation points, not the compact-domain uniformity principle missing in13.5/13.6/17.5. |
| Matrix-chain-rule remainder proof | I tried k(h)=0 and unbounded k(h)/norm(h), the usual hidden gaps. Actual8.2 source206 explicitly supplies the Frobenius norm bound from Cauchy–Schwarz and hence bounded k(h)/norm(h).208 handles zero k separately. For nonzero k, the displayed product of ratios gives o(norm h). | Protected. No continuity-of-derivative hypothesis is smuggled into differentiability at the two points. |
| Inverse function theorem proof status | I checked whether differentiating the inverse identity is used circularly to prove existence/smoothness.8.5 source231–251 supplies the exact local existence/C1 conclusion;252–274 is titled geometric explanation;277 explicitly says existence and smoothness are supplied, and only the derivative formula follows after a differentiable inverse is known. | Protected supplied root. A full contraction-map proof is not owed by the present contract. |
| Two-equation implicit review | I tried to defeat the n=4 application on grounds that only a2D implicit theorem was taught. `c8s7-sk-system-solvable`678–700 supplies Phi=(x,y,F,G). Its derivative determinant reduces to det[[2,1],[1,-2]]=-5 at(1,1,1,1), inside the supplied arbitrary-n IFT scope. Setting last output coordinates0 gives u,v. Differentiating gives2ux+vx=-1 and ux-2vx=0, so ux=-2/5,vx=-1/5. | Supported task, with an operational hint rather than a demand for an unprovided general implicit-function theorem. |
| Arc-length proof ceiling | I tried to count5.3 as an earlier complete compactness proof, which would defeat the shared gap. Source266–291 states the length theorem but labels the argument “Proof idea”;288–289 asserts uniform behavior rather than proving a finite-cover/subsequence principle. | It supports using the arc-length formula, not the later proof toolkit. Do not convert an explicitly limited sketch into the missing supporting theorem. |

## Cross-scope and challenge limits

The foundations negative-support conclusions are consistent with the source alternatives checked here: neither the MVT remainder arguments nor the arc-length sketch proves a finite-cover/subsequence theorem. Relative connectedness cannot be inferred merely from a two-sheet geometric example. However, a targeted challenge cannot independently certify book-wide absence; those negative claims rely on the primary workers' complete distributed source reads and the actual positive-support search record. Root owns the combined compactness repair and its severity.

I did not find a reason to retract the three foundations observations or the protected results challenged here. The strongest successful objection was to interpreting FND-02 as more than P3: its context already teaches and emphasizes the missing hypothesis. The report should preserve that limit. No textbook, historical audit, generated output, commit, or remote operation was changed by this challenge.
