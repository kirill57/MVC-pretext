# Proof status, reference routes and protected material

Snapshot `9a9d110af839b539fe598de66ecc4320a4126af4`. All 40 assigned sections were semantically read, including their mathematics, tables, all 57 tagged exercises and 18 hints, and all 5 Chapter 9 figure sources. Appendices A–E have no concrete exercise/problem sets, hints, solutions or graphics. Their four mechanically selected imperative sentences are reference algorithms/setup, explicitly excluded as assessments in the witness ledger. The 15 untagged project hand-in items in Chapter 9 are included separately. No rendered visual review is claimed.

## Chapter 9 proof boundaries

| Result / source | Actual status and dependency check |
|---|---|
| `c9s1-thm-gradient-normal-level-curve`, lines149–185 | Conditional smooth-level-curve theorem with nonzero gradient. The proof is the previously available chain rule and a constant composite. It does not prove existence of the smooth level set from mere partial derivatives. |
| `c9s1-thm-gradient-normal-level-surface`, lines304–325 | Correct statement conditional on a smooth surface and nonzero gradient. The earlier scalar IFT gives a local graph under C1 hypotheses; graph tangent vectors fill the kernel. The checkpoint omission is separately REF-01. |
| `c9s2-thm-fermat`, lines229–262 | Coordinate-slice proof needs only existence of the partials for gradient zero. The text explicitly adds total differentiability before identifying `df=0`. Old M01 is resolved. |
| `c9s3-thm-equality-of-mixed-partials`, lines219–254 | Labeled proof idea; the rectangular mixed difference divided by `uv` is read by successive one-variable MVT applications. Nearby mixed-partial existence and continuity at the target justify the limit. This is sufficient at its stated proof-idea ceiling and makes no integral-exchange claim. |
| `c9s3-thm-second-order-approximation`, lines576–643 | Labeled proof idea but contains the key complete estimate: exact segment integral remainder; all four Hessian-entry differences bounded by epsilon when the entire segment is in the continuity neighborhood; `abs(R)<=epsilon(u²+v²)`. Since `norm(tu)<=norm(u)` for `0<=t<=1`, no finite-cover or subsequence theorem is required. Old M02 is resolved. |
| `c9s4-thm-second-derivative-test`, lines279–348 | Labeled proof idea supplies the norm-uniform lower bound using a positive minimum of `q(cos theta,sin theta)` on `[0,2pi]`. EVT was already explicitly stated in `c6s4-thm-extreme-value-theorem`, including dimension n. The small remainder is bounded by one quarter of that quadratic minimum. The separate `A=0, Delta<0` case is treated at216–219. Old M03 is resolved. |
| `c9s5-thm-lagrange-one-constraint`, lines203–241 and preceding186–201 | The kernel-inclusion argument is correct even for `df=0`: choose `w` with `dg(w)=1`, decompose `v=(v-dg(v)w)+dg(v)w`, and apply `df`. It does not assert equal kernels. Scalar IFT supplies local graph/tangent availability under the stated C1/nonzero-gradient assumptions. Old M04 is resolved. |
| `c9s5-thm-lagrange-two-constraints`, lines364–383 | Precise R3 theorem with independent constraint gradients, supplied for use. The local normal-plane discussion is geometric motivation, not a written general joint-constraint IFT proof. A proof can use the already supplied n-dimensional inverse-function theorem, but this audit does not invent that proof as source content. Do not silently generalize this statement to arbitrary n. |
| `c9s5-thm-extreme-value`, lines463–474 | Reuses the earlier explicitly supplied `c6s4-thm-extreme-value-theorem`. The book says the full existence proof belongs to later analysis; no in-book proof destination is promised. Restore “nonempty” as small scope cleanup. This theorem does not automatically teach finite subcovers, Bolzano–Weierstrass, or relative-open connectedness. |
| `c9s7-subsec-container`, lines984–1015 | The new all-dimensions comparison `x+y>=2sqrt(xy)` is explicit, and reduces every positive box to the square-base family without increasing area. A derivative sign check yields its unique global minimum. Old M05 is resolved. |
| `c9s7-subsec-least-squares`, lines742–888 | Error function and all normal-equation data are supplied before the report task. R4 dot products are available from `c2s4-algebraic-definition-and-first-examples:72–75`. No regression/probability theory or statistical model is required. |

## Reference routes

The appendices describe themselves as references to scan. This audit does not treat them as silently prior preparation for Chapters 1–20, nor require that they replace all the earlier lessons. The local rules are adequate for looking up the stated formulas. Capability records in `concepts.json` label first roles within each reference route rather than asserting global first mentions.

- **Appendix A:** standard single-variable operations are within the entry contract. Differential notation locally states `dx(h)=h` and evaluates `df_x(h)`, avoiding a covector/scalar equality (L29 resolved). FTC II gives continuity/endpoint assumptions (L30 resolved). Polar area says traced once; the curve-orientation warning specifically concerns work integrals (L31 resolved). Series defines partial sums, includes the zero-coefficient geometric-series exception (L32 resolved), and supplies Taylor’s remainder. No task needs unannounced series convergence tests. Its Stokes boundary comparison is a reference/forward connection, not a promised proof of generalized Stokes.
- **Appendix B:** real algebra and trigonometry are entry capabilities. Complex arithmetic, conjugate, modulus, polar form, Euler notation and roots are explicitly supplied in the reference. Roots follow by dividing angles modulo `2pi`, and the roots-of-unity sum uses the finite geometric identity with `n>1`. No branch of complex logarithm or complex differentiation is used.
- **Appendix C:** matrix operations, dimensions, elementary row operations, determinant/inverse conditions, eigenpairs and symmetric quadratic forms are all stated. The general determinant is explicitly cross-referenced to `c3s4-def-determinant-general`, not inferred from only a2x2 formula (L33 resolved). The real symmetric spectral theorem is supplied here; no local full proof is advertised. `q` has unit-direction second derivative `2lambda_i`, and the Hessian test states C2 plus critical point (L34 resolved). Its Jacobian area density is a local formula, not a demonstrated global integration theorem; invoking it over regions still uses the main-text substitution hypotheses.
- **Appendix D:** its wrapper explicitly supplies common derivative regularity, regular once-covering coordinate patches, pullback abbreviation and unsigned-density conventions. `ds=a|dtheta|` and `ds=|dr|` preserve orientation reversal (L35/L36 resolved). Curvilinear vector formulas specify the local unit basis and exclude coordinate singularities. Spherical order `(rho,phi,theta)` gives positive volume factor `rho² sin(phi)`; the unit basis `(e_rho,e_phi,e_theta)` has that orientation. The Laplacian is defined locally; no PDE solving or numerical procedure is asked.
- **Appendix E:** the wrapper states C1 for one exterior derivative, C2 for identities with two derivatives, and compatible once-coverage for integrals. Antisymmetry concerns swapping1-form factors; the general graded sign rule correctly makes a1-form commute with a2-form. Volume/area wedge expressions are explicit forms, not unsigned measures. Generalized Stokes includes compactness, orientation, piecewise smoothness, C1 on an open neighborhood and induced boundary orientation (L37 resolved). Formula summaries inherit their reference context; they are not independent entry prerequisites. No false `d²` counterexample occurs in Appendix E.

## Source-first false-positive decisions

- The Hessian symmetry checkpoint precedes the general mixed-partial theorem, but asks what equality of two entries means for a matrix. It supplies that equality as the premise and the local hill example already computes the matrix. This is not a missing Schwarz-theorem prerequisite.
- `checkpoint-9-4-quadratic-decides` is a legitimate discovery/explanation task. Its full solution can be reconstructed from the already supplied EVT and Taylor remainder; the later proof does not supply an unavailable theorem.
- A gradient flow line is defined by an equation and the chain rule computes the change in the objective along a curve satisfying it. Chapter 9 never asks the student to solve that ODE or prove existence/uniqueness. No ODE course is being assumed here.
- The multiplier-sensitivity checkpoint is supported by the explicitly computed circle family `M(c)=10sqrt(c)`. Its “often” language should not be promoted to a general differentiable-value-function theorem.
- The first mixed-partial failure example computes `F_xy=-1,F_yx=1` without exterior-derivative notation. It is correct within the elementary derivative definitions and does not itself exhibit U06’s operator-domain issue.
- Figure sources use taught labels: contour gradient/tangent, saddle sign directions, quadratic sign panels, circle multiplier normals, and a decision diagram with “Differentiable interior point” and “Regular constraint”. Source inspection reveals no additional unintroduced operator; clipping/overlap cannot be certified without rendered inspection.

## Limitations and commands actually used

Read actual HEAD and worktree: `git rev-parse HEAD`, `git branch --show-current`, `git status --short`; source is pinned as above. Initial status contained the untracked controlling brief only. Read the full controlling audit brief, both conversion operating documents, all assigned source sections in XInclude order and relevant earlier dependencies. Source extraction used local lxml to retain element source lines and all displayed mathematics/graphics code; the original source was reread for references and exact support windows. Root’s inventory/task index supplied stable IDs and hashes, not logical conclusions.

Generated only fragment files. JSON construction checks that every one of72 assigned root task candidates has a disposition and that all57 tagged exercises have witnesses. Added15 source-line project deliverables. No textbook edits, old-report edits, Git mutation or remote action occurred. No build or rendering was performed by this worker; root owns structural baseline validation. Independent challenge and book-wide graph closure remain integration work.
