# Root proof status and nonissues

All Chapters 10–13 were read sequentially, including figure source, review exercises and untagged project instructions. Rendering status is separate. Snapshot `9a9d110af839b539fe598de66ecc4320a4126af4`.

- **10.1–10.4:** signed area forms evaluate on vectors, while ordinary area measure is unsigned. Fubini is supplied for continuous integrands, with the intended elementary region conventions. Section 10.3 explicitly distinguishes the order of scalar iterated integration from the antisymmetry of `dx wedge dy`. Section 10.4 defines the general-region integral by zero extension before presenting slice bounds. These are legitimate local introductions, not scalar/form type errors.
- **10.5:** the polar formula has a sector-area derivation, an error-bound explanation, and a precise once-covering supplied statement. It does not need the later general change-of-variables theorem as an unstated prerequisite. This supports the proposed Chapter 11 slicing repair without a circular proof.
- **10.7 and 12.7 Gaussian project:** first work on bounded squares, where the supplied Fubini theorem applies. For `I_R=integral[-R,R] exp(-x^2) dx`, disk inclusion gives `pi(1-exp(-R^2)) <= I_R^2 <= pi(1-exp(-2R^2))`. Taking limits squeezes the result to `pi`, so `I=sqrt(pi)`. Scaling gives `sqrt(pi/a)` for `a>0` and normalization `1/(sigma sqrt(2pi))` for `sigma>0`. No unjustified use of improper Fubini is needed; Chapter 12 explicitly points to the bounded proof.
- **11.1 and 11.6:** the coordinate three-form uses its particular determinant definition from 2.7; it is not arbitrary triple wedge. Higher-dimensional box Fubini is explicitly supplied in the optional lesson. Required review does not silently make that optional lesson a prerequisite: the corresponding higher-dimensional review tasks are themselves identified as optional in the semantic task record.
- **12.1–12.4:** a nonlinear map can preserve area everywhere: `T(u,v)=(u,v+u^2)` has derivative `[[1,0],[2u,1]]`, determinant 1, is non-affine, and has inverse `(x,y-x^2)`. The text's checkpoint acknowledges this. General 2D/3D change-of-variables statements specify C1 neighborhood, interior nonzero Jacobian, once-coverage and region conditions. Their limiting discussion is explicitly a proof idea; the theorem is a supplied root at this level, not a full proof of measure-theoretic substitution.
- **12.5:** `(alpha wedge beta wedge gamma)(a,b,c)` is defined by the determinant of the three scalar measurements; grouping `(alpha wedge beta) wedge gamma` is explicitly explained before local tasks. Pullbacks preserve their source/target spaces. Signed formulas specify induced orientation; ordinary area/volume formulas use absolute determinants.
- **12.6:** complex squaring on a disk has multiplicity two except at its critical point: integrating `4(u^2+v^2)` over the unit parameter disk gives `2pi`, twice the image disk area `pi`. A polar angle interval `[0,4pi]` likewise doubles area. A collapsed boundary seam/axis/pole has a different role from repeated interior coverage, as the text explains.
- **13.1:** checking a candidate flow line means differentiating the given curve and comparing with the field. The text supplies equilibrium curves but does not promise existence/uniqueness or require an unintroduced ODE solver.
- **13.2–13.4:** scalar line integrals use speed and are invariant under reversal; work/1-form integrals use directed velocity and change sign. Reparametrization follows the previously taught chain rule and ordinary substitution. The angular unit vector corresponds through the Euclidean metric to `r dtheta`, not `dtheta`; the text teaches this distinction. Repeated traversal is counted.
- **13.5:** the forward fundamental theorem of line integrals is proved by chain rule plus ordinary FTC. Path independence makes the integral-defined potential single-valued. The converse differentiability proof has one unsupported compact-path subdivision step, ROOT-03; the theorem is true and no circularity is thereby alleged.
- **13.6:** the parameter-integral lemma is valid with its actual continuous derivative on a neighborhood of a rectangle; the issue is support of the uniformity argument. The rectangular potential construction correctly uses that lemma. The plane simply-connected potential theorem is explicitly supplied for use with proof promised in Section 17.5, whose compactness dependency is audited separately. Unequal mixed partials here are computed with elementary derivatives; applying exterior d outside its domain is a distinct later issue, not a defect in these computations.
- **13.7:** `alpha=(-y dx+x dy)/(x^2+y^2)` is closed on the punctured plane, but its integral around `(R cos t,R sin t)` is `2pi` for every `R>0`. It has no global single-valued potential; on `x>0`, `arctan(y/x)` is a valid branch. The singular origin prevents contraction within the domain.
- **13.8:** the plane formula for exterior derivative of a C1 1-form is introduced locally before its use. The `d(df)=0` computation assumes C2. Rectangle/circle area comparisons are direct calculations. The polygon edge calculation is complete; its shoelace area interpretation is explicitly deferred to Section 14.7. This is an honest later proof reference, not an early assumed area theorem.

These supported judgments are candidates for independent challenge, not declarations based on keyword absence. Each task witness points to exact source support in `task-witnesses.json`.
