# Independent challenge of Chapters14–15

Reviewer: root; primary reader: reference worker. Snapshot `9a9d110af839b539fe598de66ecc4320a4126af4`. Targeted challenge, not a second full16-section review.

**GS-01 challenged and retained as P2 precision repair.** I reread the opening of14.4 and its theorem at lines201–217. The strongest defense is inherited context: these are called two costumes for the same theorem. Nevertheless the fresh quantified statement says only bounded region and C1 on an open region containing R. It explicitly inherits orientation, not the earlier closed-region hypothesis. The open unit disk together with `dx/(1-x²-y²)` meets the displayed hypotheses literally while having no boundary values. A single “closed” or an explicit reference to14.2's complete hypotheses closes the problem. The correct earlier theorem and all ordinary applications remain protected. Confidence in the literal counterexample is high; confidence that readers would miss the intended inherited condition is only medium. No P1 elevation warranted.

**5.2 ->15.1 proof promise challenged and protected.** I reread15.1's definition and theorem/proof (267–287,331–373). “Smooth” here is explicitly C1. The proof goes beyond two coordinate tangents: it invokes the full vector first-order remainder `||E(h,k)||/sqrt(h²+k²)->0`. Regularity gives rank2; a nonzero2x2minor and the supplied inverse theorem provide local graph coordinates. These are precisely the scope/nearby-continuity qualifications in5.2. The later proof exists and supplies the advertised stronger justification; there is no cycle through surface integration.

**Scalar-versus-flux task challenged and protected.** The15.9 weighted-paraboloid witness uses `sqrt(1+4r²)` rather than the signed graph flux factor. Substituting `u=1+4r²` into `2pi integral_0^1(4-r²)sqrt(1+4r²)r dr` gives `pi(175sqrt5-41)/60`, confirming the worker's value. For the cone, area density `sqrt2*s` integrates over `0<=s<=3` to `9pi sqrt2`; the omitted tip contributes `pi sqrt2*epsilon²`, so no smoothness claim at the apex is required.

**Green/Stokes proof ceiling challenged and protected.** Exact rectangle/finite-simple-region arguments are distinguished from general patching sketches. A C2 chart gives the mixed-coordinate cancellation in surface naturality; the stated general theorem is supplied for use. The inherited13.6 analysis premise remains recorded under the shared compactness finding and is not falsely discharged by the later supplied theorem. This separates legitimate theorem use from a complete in-book proof.

**Deferred shoelace endpoint corrected in audit metadata.** The actual endpoint is14.7 `c14s7-ex-shoelace`, not14.3. The audit's root prose/TSV cross-reference was corrected; no textbook source was changed.
