[5-5 title sec-2-chapter-review-and-discovery-problems default_required]
Chapter review and discovery problems

[8-12 p sec-2-chapter-review-and-discovery-problems default_required]
Hamilton's multiplication led us to the dot product and cross product. Determinants
      then pulled out the deeper quantities: signed area and signed volume. The wedge
      product kept those quantities in their natural form.

[13-16 p sec-2-chapter-review-and-discovery-problems default_required]
This review is not meant to be a list of formulas to memorize. The goal is to keep the
      geometric story alive while practicing the computations.

[20-20 title ex-grp-c2s8-concept-check default_required]
Concept check

[24-27 p ex-c2s8-point-vs-vector default_required]
Explain the difference between a point and a vector. Why can the same ordered
          pair represent either one, depending on context?

[33-37 p ex-c2s8-complex-effects default_required]
Complex multiplication by
          re^{i\theta}
          has two geometric effects. What are they?

[43-46 p ex-c2s8-give-up-commutativity default_required]
Why did Hamilton have to give up commutativity when he introduced the quaternion
          units i,j,k?

[52-56 p ex-c2s8-sign-change default_required]
The quaternion rules include
          ij=k, \qquad ji=-k.
          What geometric idea is hidden in this sign change?

[62-68 p ex-c2s8-product-splits default_required]
When two pure imaginary quaternions
          U=u_1i+u_2j+u_3k, \qquad V=v_1i+v_2j+v_3k
          are multiplied, their product splits as
          UV=-\mathbf u\cdot\mathbf v+\mathbf u\times\mathbf v.
          Which part measures alignment? Which part measures oriented area?

[74-78 p ex-c2s8-dot-zero default_required]
What does
          \mathbf u\cdot\mathbf v=0
          mean geometrically, assuming neither vector is zero?

[84-88 p ex-c2s8-cross-zero default_required]
What does
          \mathbf u\times\mathbf v=\mathbf 0
          mean geometrically?

[94-97 p ex-c2s8-cross-perpendicular default_required]
Why does the cross product point perpendicular to the parallelogram spanned by
          its inputs?

[103-107 p ex-c2s8-signed-area-det default_required]
Explain why
          \det[\mathbf u\ \mathbf v]
          is signed area, not just area.

[113-117 p ex-c2s8-det-zero-box default_required]
Explain why
          \det[\mathbf u\ \mathbf v\ \mathbf w]=0
          means that the three vectors do not form a genuine three-dimensional box.

[123-126 p ex-c2s8-positively-oriented default_required]
What does it mean for an ordered basis of \mathbb R^3 to be positively
          oriented?

[132-135 p ex-c2s8-no-cross-r4 default_required]
Why is there no single honest cross product for two vectors in \mathbb R^4?
          What goes wrong with the idea of the perpendicular direction?

[141-144 p ex-c2s8-wedge-antisymmetry default_required]
What is the geometric meaning of
          dx\wedge dy=-dy\wedge dx?

[150-153 p ex-c2s8-wedge-self-zero default_required]
Why is
          dx\wedge dx=0?

[159-159 title ex-grp-c2s8-skills default_required]
Skills

[163-167 p ex-c2s8-complex-product default_required]
Compute and interpret geometrically:
          (2+3i)(1-i).
          Write the answer in rectangular form.

[173-178 p ex-c2s8-polar-form default_required]
Write
          -1+i\sqrt3
          in polar form. What rotation and scaling does multiplication by this complex number
          perform?

[184-188 p ex-c2s8-quaternion-product default_required]
Use the quaternion rules to compute
          (2i-j+3k)(i+4j+2k).
          Then identify the scalar part and the vector part.

[194-200 p ex-c2s8-dot-norms-cosine default_required]
Let
          \mathbf u=(3,4,0), \qquad \mathbf v=(1,2,2).
          Compute
          \mathbf u\cdot\mathbf v, \qquad \|\mathbf u\|, \qquad \|\mathbf v\|,
          and the cosine of the angle between them.

[206-211 p ex-c2s8-projection default_required]
Find the projection of
          \mathbf u=(3,4,0)
          onto
          \mathbf v=(1,2,2).

[217-224 p ex-c2s8-work default_required]
A sled is pulled by a force
          \mathbf F=(30,10)
          newtons through a displacement
          \mathbf d=(8,6)
          meters. Compute the work done:
          W=\mathbf F\cdot\mathbf d.

[230-234 p ex-c2s8-cross-perp-check default_required]
Compute
          (2,-1,3)\times(1,4,2).
          Then check directly that your answer is perpendicular to both input vectors.

[240-243 p ex-c2s8-normal-to-plane default_required]
Find a normal vector to the plane spanned by
          \mathbf a=(1,2,0), \qquad \mathbf b=(0,1,3).

[249-254 p ex-c2s8-plane-equation default_required]
Find an equation of the plane through
          P=(2,-1,4)
          with direction vectors
          \mathbf a=(1,2,0), \qquad \mathbf b=(0,1,3).

[260-267 p ex-c2s8-torque default_required]
A force
          \mathbf F=(0,40,0)
          is applied at position
          \mathbf r=(0.25,0,0)
          meters from a pivot. Compute the torque
          \boldsymbol\tau=\mathbf r\times\mathbf F.

[273-277 p ex-c2s8-signed-area-2x2 default_required]
Compute the signed area:
          \begin{vmatrix} 5&2\\ 1&4 \end{vmatrix}.
          Then interpret the sign.

[283-288 p ex-c2s8-triangle-area default_required]
Find the area of the triangle with vertices
          A=(1,1), \qquad B=(5,2), \qquad C=(2,6).
          Hint: first find the parallelogram area spanned by
          B-A \qquad\text{and}\qquad C-A.

[294-297 p ex-c2s8-signed-volume-3x3 default_required]
Compute the signed volume:
          \begin{vmatrix} 1&0&2\\ 2&1&0\\ 0&3&1 \end{vmatrix}.

[303-308 p ex-c2s8-tetrahedron-volume default_required]
Find the volume of the tetrahedron with vertices
          A=(0,0,0), \qquad B=(1,2,0), \qquad C=(0,1,3), \qquad D=(2,0,1).
          Hint: a tetrahedron has one-sixth the volume of the parallelepiped formed by its three
          edge vectors from one vertex.

[314-320 p ex-c2s8-grid-machine-scaling default_required]
A linear grid machine sends
          \mathbf e_1=(1,0) \quad\text{to}\quad \mathbf u=(3,1),
          and
          \mathbf e_2=(0,1) \quad\text{to}\quad \mathbf v=(2,4).
          Find the signed area-scaling factor of the machine.

[326-329 p ex-c2s8-orientation-decision default_required]
Decide whether the machine in the previous problem preserves orientation, reverses
          orientation, or collapses area.

[335-340 p ex-c2s8-wedge-on-vectors default_required]
Compute
          (dx\wedge dy)(\mathbf u,\mathbf v)
          for
          \mathbf u=(4,-1), \qquad \mathbf v=(2,3).

[346-355 p ex-c2s8-three-shadow-areas default_required]
For
          \mathbf u=(2,1,3), \qquad \mathbf v=(1,4,0),
          compute the three signed shadow areas
          (dy\wedge dz)(\mathbf u,\mathbf v),
          (dz\wedge dx)(\mathbf u,\mathbf v),
          and
          (dx\wedge dy)(\mathbf u,\mathbf v).
          Then compare them with the components of \mathbf u\times\mathbf v.

[361-368 p ex-c2s8-orientation-triples default_required]
Decide whether each ordered triple is positively oriented, negatively oriented, or
          not a basis:
          (\mathbf i,\mathbf j,\mathbf k),
          (\mathbf j,\mathbf i,\mathbf k),
          (\mathbf i,\mathbf j,\mathbf i+\mathbf j),
          (\mathbf i+\mathbf j,\mathbf j+\mathbf k,\mathbf k+\mathbf i).

[374-374 title ex-grp-c2s8-discovery default_required]
Discovery problems

[378-384 p ex-c2s8-cross-shadow-components default_required]
Let
          \mathbf u=(u_1,u_2,u_3), \qquad \mathbf v=(v_1,v_2,v_3).
          Show that the components of \mathbf u\times\mathbf v are the three signed shadow
          areas:
          \mathbf u\times\mathbf v = \bigl( (dy\wedge dz)(\mathbf u,\mathbf v),\, (dz\wedge dx)(\mathbf u,\mathbf v),\, (dx\wedge dy)(\mathbf u,\mathbf v) \bigr).

[390-395 p ex-c2s8-pythagoras-shadows default_required]
Use the previous problem to show that
          \|\mathbf u\times\mathbf v\|^2 = \bigl((dy\wedge dz)(\mathbf u,\mathbf v)\bigr)^2 + \bigl((dz\wedge dx)(\mathbf u,\mathbf v)\bigr)^2 + \bigl((dx\wedge dy)(\mathbf u,\mathbf v)\bigr)^2.
          This says that the squared area of a parallelogram in space is the sum of the squared
          areas of its coordinate shadows.

[401-405 p ex-c2s8-symmetric-part default_required]
Show directly from the quaternion product that
          UV+VU=-2\,\mathbf u\cdot\mathbf v.
          This identity says that the dot product is the symmetric part of the product.

[411-415 p ex-c2s8-antisymmetric-part default_required]
Show directly from the quaternion product that
          UV-VU=2\,\mathbf u\times\mathbf v.
          This identity says that the cross product is the antisymmetric part of the product.

[421-425 p ex-c2s8-bilinear-alternating default_required]
Let
          B(\mathbf u,\mathbf v)=u_1v_2-u_2v_1.
          Prove directly from the formula that B is bilinear and alternating.

[431-435 p ex-c2s8-bilinear-not-alternating default_required]
Let
          C(\mathbf u,\mathbf v)=u_1v_1+u_2v_2.
          Prove directly from the formula that C is bilinear but not alternating.

[441-445 p ex-c2s8-alternating-not-bilinear default_required]
Let
          A(\mathbf u,\mathbf v)=(u_1v_2-u_2v_1)^3.
          Show that A is alternating but not bilinear.

[451-457 p ex-c2s8-r4-two-normals default_required]
In \mathbb R^4, let
          \mathbf e_1=(1,0,0,0), \qquad \mathbf e_2=(0,1,0,0).
          Find two different unit vectors perpendicular to both \mathbf e_1 and
          \mathbf e_2. Explain why this prevents a unique normal-vector cross product for
          two vectors in \mathbb R^4.

[463-468 p ex-c2s8-triple-wedge-sign default_required]
The determinant changes sign when two columns are swapped. Use this fact to
          explain why
          dx\wedge dy\wedge dz
          changes sign whenever two input vectors are swapped.

[474-482 p ex-c2s8-polar-area-element default_required]
A narrow circular sector lies between radii r and r+h and spans
          an angle k radians, where r,h,k>0. The area of a sector of radius
          R and angle k is R^2k/2 (the fraction k/(2\pi)
          of a disk). Subtract the two sector areas to show that this strip has exact area
          rhk+h^2k/2. Why is rhk a good approximation when h
          is small compared with r? Explain the factor r using the inner arc
          length rk.

[488-495 p ex-c2s8-unit-square-determinant default_required]
A machine sends the unit square to the parallelogram spanned by
          \mathbf u=(a,c), \qquad \mathbf v=(b,d).
          Show that its signed area-scaling factor is
          ad-bc.
          This number will become the determinant of the matrix
          \begin{pmatrix} a&b\\ c&d \end{pmatrix}.

[501-508 p ex-c2s8-volume-scaling-det default_required]
Suppose a machine in space sends
          \mathbf i,\mathbf j,\mathbf k
          to
          \mathbf u,\mathbf v,\mathbf w.
          Explain why the signed volume-scaling factor must be
          \det[\mathbf u\ \mathbf v\ \mathbf w].

[514-520 p ex-c2s8-triple-wedge-compute default_required]
Let
          \mathbf u=(1,2,0), \qquad \mathbf v=(0,1,3), \qquad \mathbf w=(2,0,1).
          Compute
          (dx\wedge dy\wedge dz)(\mathbf u,\mathbf v,\mathbf w).
          Then swap two of the vectors and compute again. What changes? What stays the same?

[526-529 p ex-c2s8-same-volume-opposite-orientation default_required]
Find two different ordered bases of \mathbb R^3 that span boxes with the same
          ordinary volume but opposite orientation.

[535-542 p ex-c2s8-six-shadows-r4 default_required]
A parallelogram in space has side vectors \mathbf u and \mathbf v. Its
          cross product \mathbf u\times\mathbf v packages the coordinate shadow areas into
          a vector. But in \mathbb R^4, there are six coordinate-plane shadows:
          dx\wedge dy,\quad dx\wedge dz,\quad dx\wedge dw,\quad dy\wedge dz,\quad dy\wedge dw,\quad dz\wedge dw.
          What does this suggest about why a single normal vector is not enough to represent
          oriented area in four dimensions?

[548-553 p sec-2-chapter-review-and-discovery-problems default_required]
The next chapter begins with a new kind of object. Instead of measuring one pair of
      vectors at a time, we let a rule move an entire grid. A square may become a
      parallelogram, a cube may become a slanted box, and the determinant becomes the
      scale factor of the whole machine. The machine is called a matrix.
