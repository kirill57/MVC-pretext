[5-5 title sec-1-chapter-review-and-discovery-problems default_required]
Chapter review and discovery problems

[8-13 p sec-1-chapter-review-and-discovery-problems default_required]
Chapter 1 began with locations and displacements. It ended with distances to planes and
      hyperplanes. The main change was not a new formula; it was a new way to see geometry.
      Points tell us where. Vectors tell us how to move. Spans tell us what directions can
      build. Norms and distances tell us how large those motions are.

[14-17 p sec-1-chapter-review-and-discovery-problems default_required]
The problems below are meant to make those ideas usable. Some are short checks. Some are
      small discoveries. Draw pictures whenever a picture is possible.

[21-21 title ex-grp-concept-check default_required]
Concept check: vectors as objects, not just coordinates

[25-28 p ex-cc-point-vs-vector default_required]
What is the difference between a point and a vector, even when both are written with
          ordered pairs such as (3,4)?

[34-40 p ex-cc-displacement default_required]
Explain why the displacement from
          P=(2,-1,5)
          to
          Q=(7,3,1)
          is not the point Q, but the vector Q-P.

[46-49 p ex-cc-moving-arrow default_required]
The same vector can be drawn with its tail at many different points. Why does moving
          the drawing of an arrow not change the vector it represents?

[55-55 p ex-cc-linear-combination default_required]
What does it mean for a vector to be a linear combination of two other vectors?

[61-65 p ex-cc-not-two-directions default_required]
Why do the vectors
          (2,1),\qquad (4,2)
          not give two genuine directions?

[71-74 p ex-cc-independent-pair default_required]
Give an example of two vectors in \mathbb R^2 that are linearly independent.
          Explain how you know.

[80-83 p ex-cc-dependent-triple default_required]
Give an example of three vectors in \mathbb R^3 that are linearly dependent
          even though none of them is the zero vector.

[89-89 p ex-cc-basis-conditions default_required]
What two conditions must a list of vectors satisfy to be a basis?

[95-95 p ex-cc-zero-normalize default_required]
Why can the zero vector not be normalized?

[101-106 p ex-cc-sphere-vs-ball default_required]
What is the difference between the sphere
          (x-1)^2+(y+2)^2=9
          and the ball
          (x-1)^2+(y+2)^2\leq 9?

[112-115 p ex-cc-plane-descriptions default_required]
A plane can be described parametrically or by a normal equation. What does each
          description make easy to do?

[121-125 p ex-cc-degenerate-plane default_required]
Why is the equation
          0x+0y+0z=4
          not the equation of a plane?

[131-131 title ex-grp-skills default_required]
Skills: vectors, distances, lines, and planes

[135-140 p ex-sk-vector-arithmetic default_required]
Let
          \mathbf u=(3,-1,4),\qquad \mathbf v=(-2,5,0).
          Compute
          \mathbf u+\mathbf v,\qquad 2\mathbf u-3\mathbf v,\qquad -\mathbf u.

[146-149 p ex-sk-norms default_required]
Find the norm of each vector:
          (5,12),\qquad (2,-1,2),\qquad (1,1,1,1).

[155-160 p ex-sk-distance default_required]
Find the distance between
          P=(1,2,3)
          and
          Q=(5,-1,9).

[166-170 p ex-sk-sphere-equation default_required]
Find the equation of the sphere centered at
          C=(2,-1,4)
          with radius 6.

[176-179 p ex-sk-sphere-center-radius default_required]
Find the center and radius of the sphere
          (x-3)^2+y^2+(z+5)^2=49.

[185-188 p ex-sk-normalize default_required]
Normalize the vector
          \mathbf v=(4,0,-3).

[194-199 p ex-sk-line-parametric default_required]
Write a parametric equation of the line through
          P=(1,0,2)
          in the direction
          \mathbf u=(3,-1,4).

[205-210 p ex-sk-point-on-line default_required]
Does the point
          (7,-2,10)
          lie on the line
          (x,y,z)=(1,0,2)+t(3,-1,4)?

[216-221 p ex-sk-plane-parametric default_required]
Write a parametric equation of the plane through
          P=(0,1,2)
          in the directions
          \mathbf u=(1,0,3),\qquad \mathbf v=(2,1,0).

[227-230 p ex-sk-three-points-plane default_required]
Find three points on the plane
          (x,y,z)=(0,1,2)+s(1,0,3)+t(2,1,0).

[236-241 p ex-sk-normal-equation default_required]
Find a normal equation for the plane through
          P_0=(1,2,3)
          with normal vector
          \mathbf n=(2,-1,5).

[247-252 p ex-sk-point-on-plane default_required]
Does the point
          (3,4,1)
          lie on the plane
          2x-y+5z=9?

[258-262 p ex-sk-parametrize-plane default_required]
Parametrize the plane
          x+2y-z=4
          by choosing y=s and z=t.

[268-273 p ex-sk-distance-to-plane default_required]
Find the distance from
          A=(1,2,3)
          to the plane
          2x-y+2z=7.

[279-285 p ex-sk-closest-point-line default_required]
Find the closest point on the line
          L=(0,0)+t(2,1)
          to the point
          A=(3,4).
          Then find the distance from A to L.

[291-291 title ex-grp-discovery default_required]
Discovery: why two independent directions make a plane

[294-297 p ex-grp-discovery default_required]
These problems are meant to be explored slowly. They explain why linear independence is
        not just a definition, but the hidden rule behind coordinates.

[302-310 p ex-disc-solve-span default_required]
Let
          \mathbf u=(2,1),\qquad \mathbf v=(1,3).
          Try to solve
          s\mathbf u+t\mathbf v=(x,y)
          for s and t. Your answer should express s and t in terms
          of x and y. What does this prove about the span of \mathbf u and
          \mathbf v?

[316-320 p ex-disc-degenerate-span default_required]
Repeat the previous problem with
          \mathbf u=(2,1),\qquad \mathbf v=(4,2).
          Which points (x,y) can be reached? Which cannot?

[326-333 p ex-disc-plane-equation default_required]
In \mathbb R^3, let
          \mathbf u=(1,0,1),\qquad \mathbf v=(0,1,1).
          Points in the span of \mathbf u and \mathbf v have the form
          s(1,0,1)+t(0,1,1).
          Write the coordinates of such a point as (x,y,z). Then find one equation
          involving x,y,z that every such point satisfies.

[339-343 p ex-disc-translate-plane default_required]
The previous problem found a plane through the origin. Translate it through the point
          P=(2,0,1).
          Write both a parametric equation and a normal equation for the translated plane.

[349-352 p ex-disc-coplanar-dependent default_required]
Suppose three vectors in \mathbb R^3 all lie in the same plane through the
          origin. Explain why they must be linearly dependent.

[358-361 p ex-disc-independent-not-coplanar default_required]
Suppose three vectors in \mathbb R^3 are linearly independent. Explain why they
          cannot all lie in one plane through the origin.

[367-372 p ex-disc-basis-coordinates default_required]
A basis in \mathbb R^2 is like a pair of street directions. Use the basis
          \mathbf b_1=(1,1),\qquad \mathbf b_2=(1,-1)
          to express each vector below in \mathcal B-coordinates:
          (4,0),\qquad (0,6),\qquad (5,1).

[378-383 p ex-disc-draw-grid default_required]
Draw the grid formed by all points
          s(1,1)+t(1,-1)
          where s and t are integers between -3 and 3. How does this
          grid differ from the usual square coordinate grid?

[389-389 title ex-grp-hook default_required]
Hook: the need for a multiplication of vectors

[392-395 p ex-grp-hook default_required]
Addition and scaling have carried us far. But many geometric questions do not ask for
        another vector of the same kind.

[400-410 p ex-hook-work default_required]
A force
          \mathbf F=(10,0)
          pushes an object through the displacement
          \mathbf d=(3,0).
          It seems natural that the work should be 30. What should the work be if
          \mathbf d=(0,3)?
          What about
          \mathbf d=(3,4)?
          What kind of vector multiplication would answer this?

[416-423 p ex-hook-area default_required]
The vectors
          \mathbf u=(3,0),\qquad \mathbf v=(0,2)
          form a rectangle of area 6. The vectors
          \mathbf a=(3,1),\qquad \mathbf b=(1,2)
          form a slanted parallelogram. Estimate its area from a drawing. Then try to find an
          algebraic rule that gives the signed area from the four components.

[429-433 p ex-hook-orientation default_required]
Swapping the order of two directions should not change the size of the parallelogram,
          but it should reverse its orientation. What should this suggest about the sign of an
          area product?

[439-444 p ex-hook-two-products default_required]
A multiplication that measures agreement in direction should be symmetric:
          \mathbf u\cdot\mathbf v=\mathbf v\cdot\mathbf u.
          A multiplication that measures oriented area should change sign when the order is
          swapped. Why might one product not be enough?

[450-456 p sec-1-chapter-review-and-discovery-problems default_required]
The next chapter begins with this surprise: vectors have more than one useful kind of
      multiplication. One product measures how much two directions line up. Another measures the
      oriented area or volume they create. Those products will eventually lead to the
      dx\wedge dy that appears in differential forms, where signed area becomes an
      integrand rather than just a number.
