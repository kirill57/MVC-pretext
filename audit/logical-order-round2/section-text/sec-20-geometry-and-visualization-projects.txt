[5-5 title sec-20-geometry-and-visualization-projects default_required]
Geometry and visualization projects

[8-12 p sec-20-geometry-and-visualization-projects default_required]
Geometry was the first language of the book. Before derivatives, integrals, and forms,
      we needed points, vectors, curves, surfaces, and coordinates. These projects return to
      that beginning, but now with the whole course available.

[13-17 p sec-20-geometry-and-visualization-projects default_required]
The goal is not to make pretty pictures only. A picture should answer a question:
      Where does the surface bend? Where does a parametrization collapse? Which direction
      is normal? What does a linear approximation really approximate?

[21-21 title c20s1-proj-quadric-surfaces default_required]
Project 1: build and classify quadric surfaces

[23-31 p c20s1-proj-quadric-surfaces default_required]
A quadric surface is described by a second-degree equation in x,y,z. In
      , we learned the main trick:
      
        \boxed{
            \text{To understand a quadric surface, slice it.}
        }

[32-35 p c20s1-proj-quadric-surfaces default_required]
A surface that looks mysterious in three dimensions often becomes clear when we set
      one variable equal to a constant and study the resulting trace.

[37-55 p c20s1-proj-quadric-surfaces default_required]
A seed example: a shifted ellipsoid.
      Consider
      
        \frac{(x-1)^2}{9}+\frac{y^2}{4}+(z+2)^2=1.
      
      The shifted terms tell us the center:
      
        (1,0,-2).
      
      The denominators tell us the semiaxis lengths:
      
        3 \text{ in the }x\text{-direction},\qquad
        2 \text{ in the }y\text{-direction},\qquad
        1 \text{ in the }z\text{-direction}.
      
      So the surface is an ellipsoid, stretched most in the x-direction and least in the
      z-direction.

[57-57 p c20s1-proj-quadric-surfaces default_required]
A few slices confirm this.

[58-64 p c20s1-proj-quadric-surfaces default_required]
If z=-2, then
      
        \frac{(x-1)^2}{9}+\frac{y^2}{4}=1,
      
      an ellipse in the horizontal plane through the center.

[65-71 p c20s1-proj-quadric-surfaces default_required]
If y=0, then
      
        \frac{(x-1)^2}{9}+(z+2)^2=1,
      
      an ellipse in the xz-plane.

[72-78 p c20s1-proj-quadric-surfaces default_required]
If x=1, then
      
        \frac{y^2}{4}+(z+2)^2=1,
      
      an ellipse in the yz-plane.

[79-81 p c20s1-proj-quadric-surfaces default_required]
The surface was not guessed from a memorized shape. It was read from its slices.

[84-84 caption c20s1-fig-ellipsoid-traces default_required]
Two traces of the same ellipsoid, drawn in coordinates centered at (1,0,-2). The traces reveal the stretches.

[86-106 latex-image c20s1-fig-ellipsoid-traces default_required]
\begin{tikzpicture}[scale=0.78, >=stealth]
    \begin{scope}
        \draw[->] (-3.6,0) -- (3.8,0) node[below left] {\(x-1\)};
        \draw[->] (0,-2.2) -- (0,2.4) node[above] {\(y\)};
        \draw[thick] (0,0) ellipse (2.7 and 1.55);
        \fill (0,0) circle (2pt);
        \node[below] at (0,0) {center};
        \node at (0,-2.75) {slice \(z=-2\)};
    \end{scope}

    \begin{scope}[shift={(7.4,0)}]
        \draw[->] (-3.6,0) -- (3.8,0) node[right] {\(x-1\)};
        \draw[->] (0,-2.2) -- (0,2.4) node[above] {\(z+2\)};
        \draw[thick] (0,0) ellipse (2.7 and 0.95);
        \fill (0,0) circle (2pt);
        \node[below] at (0,0) {center};
        \node at (0,-2.75) {slice \(y=0\)};
    \end{scope}
\end{tikzpicture}

[110-113 p c20s1-proj-quadric-surfaces default_required]
Your task.
      Create a visual gallery of at least six quadric surfaces.

[114-129 p c20s1-proj-quadric-surfaces default_required]
Use six or more of the following:
      
        \begin{array}{c}
        x^2+y^2=4,\\[1mm]
        y=x^2,\\[1mm]
        z=x^2+y^2,\\[1mm]
        z=x^2-y^2,\\[1mm]
        \dfrac{x^2}{9}+\dfrac{y^2}{4}+z^2=1,\\[4mm]
        x^2+y^2-z^2=1,\\[1mm]
        z^2-x^2-y^2=1,\\[1mm]
        z^2=x^2+y^2,\\[1mm]
        \dfrac{(x-1)^2}{4}+\dfrac{(y+2)^2}{9}+\dfrac{z^2}{16}=1.
        \end{array}

[130-141 p c20s1-proj-quadric-surfaces default_required]
For each surface, include:
      
        \begin{array}{ll}
        1. & \text{the equation,}\\
        2. & \text{the name of the surface,}\\
        3. & \text{three traces or cross-sections,}\\
        4. & \text{a hand sketch, physical model, or computer plot,}\\
        5. & \text{one sentence explaining how signs, coefficients, or missing variables shape it.}
        \end{array}

[143-143 p c20s1-proj-quadric-surfaces default_required]
Questions to answer.

[145-147 li c20s1-proj-quadric-surfaces default_required]
Which surfaces are connected? Which have two separate pieces?

[148-153 li c20s1-proj-quadric-surfaces default_required]
Which equations contain a missing variable? What geometric effect does that
          missing variable have?

[154-159 li c20s1-proj-quadric-surfaces default_required]
Which surfaces have horizontal traces that are circles? Which have horizontal
          traces that are ellipses? Which have hyperbolas?

[160-171 li c20s1-proj-quadric-surfaces default_required]
What changes when
          
            x^2+y^2-z^2=1
          
          is replaced by
          
            z^2-x^2-y^2=1?

[172-179 li c20s1-proj-quadric-surfaces default_required]
What changes when an equation is shifted, as in
          
            \frac{(x-1)^2}{4}+\frac{(y+2)^2}{9}+\frac{z^2}{16}=1?

[182-194 p c20s1-proj-quadric-surfaces default_required]
What to hand in.
      Write a short illustrated report. It should include a table like this:
      
        \begin{array}{c|c|c|c}
        \text{Equation} & \text{Name} & \text{Useful traces} & \text{Geometry note} \\ \hline
        z=x^2+y^2 & \text{elliptic paraboloid} &
        z=c & \text{horizontal ellipses grow upward}\\[1mm]
        z=x^2-y^2 & \text{hyperbolic paraboloid} &
        x=0,\ y=0 & \text{one slice bends up, one bends down}
        \end{array}

[195-198 p c20s1-proj-quadric-surfaces default_required]
A good gallery should make a reader feel that a quadric equation is not a code to
      memorize. It is a set of instructions for building a shape.

[202-202 title c20s1-proj-tangent-planes default_required]
Project 2: visualize tangent planes and linear approximations

[204-219 p c20s1-proj-tangent-planes default_required]
gave the tangent plane formula for a differentiable graph
      
        z=f(x,y).
       At the point
      
        (a,b,f(a,b)),
      
      the tangent plane is
      
        \boxed{
            z=f(a,b)+f_x(a,b)(x-a)+f_y(a,b)(y-b).
        }
      
      This plane is the best first-order model of the surface near the point.

[220-223 p c20s1-proj-tangent-planes default_required]
The word near matters. A tangent plane is not a replacement for the whole surface.
      It is a local microscope.

[225-265 p c20s1-proj-tangent-planes default_required]
A seed example: a curved roof.
      Suppose a small roof has height
      
        f(x,y)=5+\frac12x-\frac14y+\frac18x^2-\frac1{16}y^2+\frac18xy.
      
      At
      
        (a,b)=(2,1),
      
      the height is
      
        \begin{aligned}
            f(2,1)
            &=
            5+1-\frac14+\frac12-\frac1{16}+\frac14\\
            &=
            \frac{103}{16}.
        \end{aligned}
      
      The partial derivatives are
      
        f_x(x,y)=\frac12+\frac14x+\frac18y,
      
      and
      
        f_y(x,y)=-\frac14-\frac18y+\frac18x.
      
      So
      
        f_x(2,1)=\frac98,
        \qquad
        f_y(2,1)=-\frac18.
      
      The tangent plane is
      
        \boxed{
            z=\frac{103}{16}+\frac98(x-2)-\frac18(y-1).
        }

[266-287 p c20s1-proj-tangent-planes default_required]
Now compare the true height with the plane at a nearby point, say
      
        (2.2,0.9).
      
      The tangent plane predicts
      
        L(2.2,0.9)
        =
        \frac{103}{16}
        +
        \frac98(0.2)
        -
        \frac18(-0.1).
      
      The actual error is
      
        f(2.2,0.9)-L(2.2,0.9).
      
      If we move the point twice as close to (2,1), the error should become much smaller.
      That is what differentiability means: the linear model wins at small scales.

[290-290 caption c20s1-fig-tangent-slice default_required]
A tangent plane is seen in every slice as a tangent line.

[292-312 latex-image c20s1-fig-tangent-slice default_required]
\begin{tikzpicture}[scale=0.82, >=stealth]
    \draw[->] (-0.5,0) -- (5.4,0) node[right] {\(x\)};
    \draw[->] (0,-0.5) -- (0,4.5) node[above] {\(z\)};

    \draw[thick, domain=0.2:4.7, samples=80, smooth, variable=\x]
        plot ({\x},{1.3+0.8*\x-0.10*\x*\x});

    % Corrected tangent line based on z = 0.28x + 1.976
    \draw[blue!60!black, very thick] (1.0,2.256) -- (4.2,3.152);
    \node[blue!60!black, right] at (4.2,3.152) {tangent line in one slice};

    % Corrected point coordinates
    \fill (2.6,2.704) circle (2.5pt);
    \node[above left] at (2.6,2.704) {point};

    % Corrected dashed line height
    \draw[dashed] (2.6,0) -- (2.6,2.704);
    \node at (2.7,-0.8) {one slice of a tangent-plane picture};
\end{tikzpicture}

[316-319 p c20s1-proj-tangent-planes default_required]
Your task.
      Choose one smooth surface and one nonsmooth surface.

[320-329 p c20s1-proj-tangent-planes default_required]
For the smooth surface, use one of these or choose your own:
      
        \begin{array}{c}
        f_1(x,y)=4+x-\frac12y-\frac14x^2-\frac18y^2,\\[1mm]
        f_2(x,y)=5+\frac12x-\frac14y+\frac18x^2-\frac1{16}y^2+\frac18xy,\\[1mm]
        f_3(x,y)=\sin x\cos y.
        \end{array}

[330-338 p c20s1-proj-tangent-planes default_required]
For the nonsmooth surface, use one of these:
      
        \begin{array}{c}
        g_1(x,y)=\sqrt{x^2+y^2},\\[1mm]
        g_2(x,y)=|x|+|y|.
        \end{array}

[340-343 p c20s1-proj-tangent-planes default_required]
Part A. The smooth surface.
      Pick a point (a,b). Then:

[345-352 li c20s1-proj-tangent-planes default_required]
Compute
          
            f(a,b),\qquad f_x(a,b),\qquad f_y(a,b).

[353-362 li c20s1-proj-tangent-planes default_required]
Write the linear approximation
          
            \boxed{
                L(x,y)=f(a,b)+f_x(a,b)(x-a)+f_y(a,b)(y-b).
            }

[363-375 li c20s1-proj-tangent-planes default_required]
Plot the surface
          
            z=f(x,y)
          
          and the tangent plane
          
            z=L(x,y)
          
          on the same axes.

[376-387 li c20s1-proj-tangent-planes default_required]
Choose several nearby displacement vectors
          
            \mathbf h=(h,k).
          
          For each one, compute the error
          
            E(\mathbf h)=f(a+h,b+k)-L(a+h,b+k).

[388-401 li c20s1-proj-tangent-planes default_required]
Make a table containing
          
            \|\mathbf h\|,
            \qquad
            |E(\mathbf h)|,
            \qquad
            \frac{|E(\mathbf h)|}{\|\mathbf h\|},
            \qquad
            \frac{|E(\mathbf h)|}{\|\mathbf h\|^2}.

[403-409 p c20s1-proj-tangent-planes default_required]
The first ratio should get small as
      
        \|\mathbf h\|\to0.
      
      That is the signature of differentiability.

[411-421 p c20s1-proj-tangent-planes default_required]
Part B. The nonsmooth surface.
      At the origin, draw or plot
      
        z=\sqrt{x^2+y^2}
      
      or
      
        z=|x|+|y|.

[422-422 p c20s1-proj-tangent-planes default_required]
Then answer:

[424-426 li c20s1-proj-tangent-planes default_required]
Does the surface look like it has one tangent plane at the origin?

[427-429 li c20s1-proj-tangent-planes default_required]
What do the slices along the x-axis and y-axis look like?

[430-432 li c20s1-proj-tangent-planes default_required]
Why is a tangent plane not the same thing as matching a few coordinate slices?

[433-439 li c20s1-proj-tangent-planes default_required]
How does the picture explain the warning from : partial
          information in some directions does not guarantee a true linear approximation in all
          directions?

[442-445 p c20s1-proj-tangent-planes default_required]
What to hand in.
      Your report should include:

[447-449 li c20s1-proj-tangent-planes default_required]
the smooth surface and point you chose,

[450-452 li c20s1-proj-tangent-planes default_required]
the tangent plane calculation,

[453-455 li c20s1-proj-tangent-planes default_required]
one plot or hand sketch showing the surface and tangent plane together,

[456-458 li c20s1-proj-tangent-planes default_required]
an error table,

[459-461 li c20s1-proj-tangent-planes default_required]
one plot or hand sketch of the nonsmooth surface,

[462-467 li c20s1-proj-tangent-planes default_required]
a paragraph explaining why the smooth surface has a local linear model and the
          nonsmooth surface does not.

[469-472 p c20s1-proj-tangent-planes default_required]
The main lesson is visual: differentiability means that the graph becomes flatter and
      flatter under a microscope. A corner, cusp, or cone tip refuses to become one plane.

[476-476 title c20s1-proj-gradient-fields default_required]
Project 3: plot gradient fields and contour maps together

[478-493 p c20s1-proj-gradient-fields default_required]
A contour map shows level sets:
      
        f(x,y)=c.
      
      A gradient field shows vectors:
      
        \nabla f(x,y).
      
       connected them:
      
        \boxed{
            \nabla f \text{ is perpendicular to the level curves of } f.
        }

[494-494 p c20s1-proj-gradient-fields default_required]
This project asks you to draw both at once.

[496-515 p c20s1-proj-gradient-fields default_required]
A seed example: temperature on a plate.
      Let
      
        T(x,y)=20+4x-y-x^2-\frac12y^2.
      
      Then
      
        \nabla T(x,y)=(4-2x,\,-1-y).
      
      At the point
      
        (1,-1),
      
      we get
      
        \nabla T(1,-1)=(2,0).
      
      So the temperature rises fastest toward the positive x-direction.

[516-530 p c20s1-proj-gradient-fields default_required]
The level curve through (1,-1) has tangent directions (u,v) satisfying
      
        \nabla T(1,-1)\cdot(u,v)=0.
      
      Thus
      
        (2,0)\cdot(u,v)=0,
      
      so
      
        u=0.
      
      The tangent direction is vertical. The gradient is horizontal. They are perpendicular.

[533-534 caption c20s1-fig-gradient-contours default_required]
Contours and gradient arrows carry different information, but they fit
      together geometrically.

[536-557 latex-image c20s1-fig-gradient-contours default_required]
\begin{tikzpicture}[scale=0.78, >=stealth]
    \draw[->] (-1.2,0) -- (5.2,0) node[right] {\(x\)};
    \draw[->] (0,-4.0) -- (0,2.5) node[above] {\(y\)};

    \foreach \r in {0.8,1.4,2.0,2.6} {
        \draw[gray!45, thick] (2,-1) ellipse ({0.8*\r} and {0.8*sqrt(2)*\r});
    }

    % Arrows are 0.2 times grad T = (4-2x,-1-y).
    \foreach \x/\y in {0/0,1/0,2/0,3/0,4/0,0/-1,1/-1,2/-1,3/-1,4/-1,0/-2,1/-2,2/-2,3/-2,4/-2} {
        \draw[->, blue!65!black, thick]
            (\x,\y) -- ++({0.4*(2-\x)},{0.2*(-1-\y)});
    }

    \fill (1,-1) circle (2.4pt);
    \draw[->, red!70!black, very thick] (1,-1) -- (2.3,-1)
        node[above] {\(\nabla T(1,-1)\)};

    \node at (2,-4.55) {gradient arrows cross contour curves at right angles};
\end{tikzpicture}

[561-573 p c20s1-proj-gradient-fields default_required]
Your task.
      Choose two scalar fields from the list below:
      
        \begin{array}{c}
        f_1(x,y)=4-\dfrac{x^2}{4}-y^2,\\[3mm]
        f_2(x,y)=xy,\\[1mm]
        f_3(x,y)=6+2x-y-\dfrac14x^2-\dfrac12y^2,\\[3mm]
        f_4(x,y)=\sin x+\cos y,\\[1mm]
        f_5(x,y)=\ln\sqrt{x^2+y^2}\quad\text{on }\mathbb R^2\setminus\{(0,0)\}.
        \end{array}

[574-574 p c20s1-proj-gradient-fields default_required]
For each chosen field:

[576-583 li c20s1-proj-gradient-fields default_required]
Compute
          
            \nabla f.

[584-591 li c20s1-proj-gradient-fields default_required]
Draw several level curves
          
            f(x,y)=c.

[592-594 li c20s1-proj-gradient-fields default_required]
Draw gradient arrows on the same picture.

[595-604 li c20s1-proj-gradient-fields default_required]
Pick three points on three different level curves. At each point, show that the
          gradient is perpendicular to the level curve. You may do this by using
          
            f_x\,dx+f_y\,dy=0
          
          for tangent directions.

[605-612 li c20s1-proj-gradient-fields default_required]
Find the critical points, if any:
          
            \nabla f=\mathbf 0.

[613-618 li c20s1-proj-gradient-fields default_required]
Where possible, use the Hessian from  to classify the critical
          points.

[621-633 p c20s1-proj-gradient-fields default_required]
Extra exploration: following the arrows.
      A path
      
        \mathbf r(t)
      
      that follows the gradient field satisfies
      
        \mathbf r'(t)=\nabla f(\mathbf r(t)).
      
      Wherever the gradient is nonzero, such a path heads in the direction of greatest
      increase per unit distance.

[634-636 p c20s1-proj-gradient-fields default_required]
For one of your chosen fields, sketch a few paths that follow the gradient arrows.

[637-637 p c20s1-proj-gradient-fields default_required]
Then answer:

[639-641 li c20s1-proj-gradient-fields default_required]
Do the paths cross level curves at right angles?

[642-644 li c20s1-proj-gradient-fields default_required]
Do they move toward a maximum, away from a minimum, or through a saddle?

[645-647 li c20s1-proj-gradient-fields default_required]
What happens near a critical point?

[648-656 li c20s1-proj-gradient-fields default_required]
If your field is
          
            f_5(x,y)=\ln\sqrt{x^2+y^2},
          
          what role does the missing origin play?

[659-662 p c20s1-proj-gradient-fields default_required]
What to hand in.
      Your report should include:

[664-666 li c20s1-proj-gradient-fields default_required]
two combined contour-gradient plots,

[667-669 li c20s1-proj-gradient-fields default_required]
the gradient formulas,

[670-672 li c20s1-proj-gradient-fields default_required]
at least three perpendicularity checks,

[673-675 li c20s1-proj-gradient-fields default_required]
critical point calculations,

[676-678 li c20s1-proj-gradient-fields default_required]
Hessian classifications where possible,

[679-684 li c20s1-proj-gradient-fields default_required]
a paragraph comparing the two fields. Which one looks like a bowl, a hill, a
          saddle, a wave, or a singular field?

[686-689 p c20s1-proj-gradient-fields default_required]
The goal is to make the gradient feel less like a formula and more like a geometric
      compass.

[693-693 title c20s1-proj-parameter-grids default_required]
Project 4: compare parameter grids on a sphere, torus, and cone

[695-702 p c20s1-proj-parameter-grids default_required]
A parametrization is a coordinate system on a surface. It does more than draw the
      surface. It creates a grid, gives tangent directions, chooses an orientation, and
      supplies the area scale factor
      
        \|\mathbf r_u\times \mathbf r_v\|.

[703-706 p c20s1-proj-parameter-grids default_required]
Different parametrizations reveal different parts of the geometry. They also hide
      different parts.

[708-711 p c20s1-proj-parameter-grids default_required]
The three surfaces.
      Use the following parametrizations.

[713-728 p c20s1-proj-parameter-grids default_required]
1. Sphere of radius a.
      
        \mathbf r_S(\theta,\phi)
        =
        (a\sin\phi\cos\theta,\,
         a\sin\phi\sin\theta,\,
         a\cos\phi),
      
      where
      
        0\le\theta\le2\pi,
        \qquad
        0\le\phi\le\pi.

[730-746 p c20s1-proj-parameter-grids default_required]
2. Torus.
      Let R>r>0. The torus with major radius R and minor radius r is
      
        \mathbf r_T(\theta,\phi)
        =
        \bigl((R+r\cos\phi)\cos\theta,\,
              (R+r\cos\phi)\sin\theta,\,
              r\sin\phi\bigr),
      
      where
      
        0\le\theta\le2\pi,
        \qquad
        0\le\phi\le2\pi.

[748-763 p c20s1-proj-parameter-grids default_required]
3. Upper cone.
      
        \mathbf r_C(\theta,s)
        =
        (s\cos\theta,\,
         s\sin\theta,\,
         s),
      
      where
      
        0\le\theta\le2\pi,
        \qquad
        0\le s\le a.

[766-766 caption c20s1-fig-surface-gallery default_required]
The same two-parameter idea produces very different surface grids. (Interactive: drag to rotate, scroll to zoom.)

[769-769 caption c20s1-fig-sphere default_required]
sphere

[771-778 asymptote c20s1-fig-sphere default_required]
// 3-D interactive (WebGL) surface.
import graph3;
        size(6cm);
        currentprojection=orthographic(5,4,3);
        surface s=surface(new triple(pair t){real u=t.x, v=t.y; return (sin(v)*cos(u), sin(v)*sin(u), cos(v));},(0,0),(2pi,pi),32,16);
        draw(s, rgb(0.55,0.75,0.95)+opacity(0.9), meshpen=rgb(0.15,0.35,0.55)+linewidth(0.2pt));

[782-782 caption c20s1-fig-torus default_required]
torus

[784-791 asymptote c20s1-fig-torus default_required]
// 3-D interactive (WebGL) surface.
import graph3;
        size(6cm);
        currentprojection=orthographic(5,4,3);
        surface s=surface(new triple(pair t){real u=t.x, v=t.y; return ((2+0.7*cos(v))*cos(u), (2+0.7*cos(v))*sin(u), 0.7*sin(v));},(0,0),(2pi,2pi),40,20);
        draw(s, rgb(0.95,0.70,0.40)+opacity(0.9), meshpen=rgb(0.55,0.35,0.10)+linewidth(0.2pt));

[795-795 caption c20s1-fig-cone default_required]
cone z=r

[797-804 asymptote c20s1-fig-cone default_required]
// 3-D interactive (WebGL) surface.
import graph3;
        size(6cm);
        currentprojection=orthographic(5,4,3);
        surface s=surface(new triple(pair t){real u=t.x, s=t.y; return (s*cos(u), s*sin(u), s);},(0,0),(2pi,2.5),32,12);
        draw(s, rgb(0.70,0.55,0.90)+opacity(0.9), meshpen=rgb(0.35,0.20,0.55)+linewidth(0.2pt));

[810-813 p c20s1-proj-parameter-grids default_required]
Your task.
      For each surface:

[815-817 li c20s1-proj-parameter-grids default_required]
Draw the parameter domain.

[818-833 li c20s1-proj-parameter-grids default_required]
Draw the two families of parameter curves:
          
            \theta=\text{constant},
            \qquad
            \phi=\text{constant}
          
          or
          
            \theta=\text{constant},
            \qquad
            s=\text{constant}.

[834-836 li c20s1-proj-parameter-grids default_required]
Compute the two tangent vectors.

[837-844 li c20s1-proj-parameter-grids default_required]
Compute the magnitude of the cross product:
          
            \|\mathbf r_u\times\mathbf r_v\|.

[845-847 li c20s1-proj-parameter-grids default_required]
Find where the parametrization repeats points.

[848-855 li c20s1-proj-parameter-grids default_required]
Find where the parametrization collapses or becomes nonregular:
          
            \mathbf r_u\times\mathbf r_v=\mathbf 0.

[856-858 li c20s1-proj-parameter-grids default_required]
Describe how the parameter grid stretches or crowds on the surface.

[861-876 p c20s1-proj-parameter-grids default_required]
Useful checkpoints.
      For the sphere,
      
        \boxed{
            \|\mathbf r_{S,\theta}\times\mathbf r_{S,\phi}\|
            =
            a^2\sin\phi.
        }
      
      The factor vanishes at the poles
      
        \phi=0,\qquad \phi=\pi.
      
      All longitude lines meet there.

[877-888 p c20s1-proj-parameter-grids default_required]
For the torus,
      
        \boxed{
            \|\mathbf r_{T,\theta}\times\mathbf r_{T,\phi}\|
            =
            r(R+r\cos\phi).
        }
      
      Since R>r, this never vanishes. The grid is regular, but the outer side of the
      torus has larger area scale than the inner side.

[889-917 p c20s1-proj-parameter-grids default_required]
For the cone,
      
        \mathbf r_{C,\theta}=(-s\sin\theta,s\cos\theta,0),
      
      and
      
        \mathbf r_{C,s}=(\cos\theta,\sin\theta,1).
      
      Thus
      
        \boxed{
            \mathbf r_{C,\theta}\times\mathbf r_{C,s}
            =
            (s\cos\theta,s\sin\theta,-s),
        }
      
      so
      
        \boxed{
            \|\mathbf r_{C,\theta}\times\mathbf r_{C,s}\|=s\sqrt2.
        }
      
      The factor vanishes at
      
        s=0.
      
      That is the cone tip.

[919-930 p c20s1-proj-parameter-grids default_required]
What to hand in.
      Make one comparison table:
      
        \begin{array}{c|c|c|c}
        \text{Surface} & \text{Area scale} & \text{Repeats?} & \text{Collapses?} \\ \hline
        \text{sphere} & a^2\sin\phi & \theta=0,2\pi & \text{poles}\\[1mm]
        \text{torus} & r(R+r\cos\phi) & \theta,\phi \text{ periodic} & \text{no, if }R>r\\[1mm]
        \text{cone} & s\sqrt2 & \theta=0,2\pi & \text{tip}
        \end{array}

[931-938 p c20s1-proj-parameter-grids default_required]
Then write a short explanation of this sentence:
      
        \boxed{
            \text{A parametrization is not just a drawing; it is a measurement system.}
        }

[942-947 p sec-20-geometry-and-visualization-projects default_required]
The geometry projects end with pictures, but the next question is already present in
      the pictures. Once a surface has shape, it is natural to ask where it is highest, where
      it is lowest, where it balances, and how to choose the best point under restrictions.
      That question pulls us back into optimization.
