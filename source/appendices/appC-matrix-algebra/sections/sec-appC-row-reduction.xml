<?xml version="1.0" encoding="UTF-8"?>
<!-- Converted from multivariable_calculus/appendices/appC-matrix-algebra.tex, Section C.2.
     Status: CONVERTED. -->
<section xml:id="sec-appC-row-reduction" xmlns:xi="http://www.w3.org/2001/XInclude">
  <title>Row reduction</title>

  <subsection xml:id="appC-row-linear-systems">
    <title>Linear systems</title>

    <p>
      A system of linear equations can be written as
      <md>\boxed{A\mathbf x=\mathbf b.}</md>
    </p>

    <p>
      The augmented matrix is
      <md>\boxed{[A\mid \mathbf b].}</md>
    </p>
  </subsection>

  <subsection xml:id="appC-row-elementary-row-operations">
    <title>Elementary row operations</title>

    <p>The allowed row operations are:</p>

    <ol>
      <li><p>Swap two rows.</p></li>
      <li><p>Multiply a row by a nonzero scalar.</p></li>
      <li><p>Add a multiple of one row to another row.</p></li>
    </ol>

    <p>These operations do not change the solution set of the system.</p>
  </subsection>

  <subsection xml:id="appC-row-row-echelon-form">
    <title>Row echelon form</title>

    <p>A matrix is in row echelon form if:</p>

    <ol>
      <li><p>All zero rows are at the bottom.</p></li>
      <li><p>Each leading entry is to the right of the leading entry above it.</p></li>
      <li><p>All entries below a leading entry are zero.</p></li>
    </ol>
  </subsection>

  <subsection xml:id="appC-row-reduced-row-echelon-form">
    <title>Reduced row echelon form</title>

    <p>A matrix is in reduced row echelon form if it is in row echelon form and:</p>

    <ol>
      <li><p>Each leading entry is <m>1</m>.</p></li>
      <li><p>Each leading <m>1</m> is the only nonzero entry in its column.</p></li>
    </ol>
  </subsection>

  <subsection xml:id="appC-row-pivot">
    <title>Pivot</title>

    <p>A pivot is a leading nonzero entry in a row.</p>

    <p>A pivot column is a column containing a pivot.</p>

    <p>Variables corresponding to pivot columns are pivot variables.</p>

    <p>Variables corresponding to non-pivot columns are free variables.</p>
  </subsection>

  <subsection xml:id="appC-row-rank">
    <title>Rank</title>

    <p>
      The rank of a matrix is the number of pivots:
      <md>\boxed{\operatorname{rank}(A)=\text{number of pivot columns in }A.}</md>
    </p>
  </subsection>

  <subsection xml:id="appC-row-consistency-test">
    <title>Consistency test</title>

    <p>
      For the system
      <md>A\mathbf x=\mathbf b,</md>
      row reduce
      <md>[A\mid \mathbf b].</md>
    </p>

    <p>
      If a row has the form
      <md>\boxed{0\quad 0\quad \cdots\quad 0\mid c}</md>
      with
      <md>c\ne0,</md>
      then the system has no solution.
    </p>

    <p>Otherwise, the system is consistent.</p>
  </subsection>

  <subsection xml:id="appC-row-number-of-solutions">
    <title>Number of solutions</title>

    <p>For a consistent system:</p>

    <md>
      \begin{array}{c|c}
      \text{Situation} \amp \text{Solution type} \\ \hline
      \text{pivot in every variable column} \amp \text{unique solution}\\[1mm]
      \text{at least one free variable} \amp \text{infinitely many solutions}
      \end{array}
    </md>
  </subsection>

  <subsection xml:id="appC-row-homogeneous-systems">
    <title>Homogeneous systems</title>

    <p>
      A homogeneous system has the form
      <md>\boxed{A\mathbf x=\mathbf 0.}</md>
    </p>

    <p>
      It is always consistent because
      <md>\mathbf x=\mathbf 0</md>
      is always a solution.
    </p>

    <p>This is called the trivial solution.</p>

    <p>
      A homogeneous system has a nontrivial solution exactly when there is at least one free
      variable.
    </p>
  </subsection>

  <subsection xml:id="appC-row-null-space">
    <title>Null space</title>

    <p>
      The null space of <m>A</m> is
      <md>\boxed{\operatorname{Null}(A)=\{\mathbf x:A\mathbf x=\mathbf 0\}.}</md>
    </p>

    <p>It is the set of vectors that <m>A</m> sends to zero.</p>
  </subsection>

  <subsection xml:id="appC-row-column-space">
    <title>Column space</title>

    <p>
      The column space of <m>A</m> is
      <md>\boxed{\operatorname{Col}(A)=\text{span of the columns of }A.}</md>
    </p>

    <p>
      The equation
      <md>A\mathbf x=\mathbf b</md>
      has a solution exactly when
      <md>\boxed{\mathbf b\in\operatorname{Col}(A).}</md>
    </p>
  </subsection>

  <warning xml:id="appC-row-common-warnings">
    <title>Common warnings</title>
    <p>
      Row operations preserve the solution set of a linear system, but they do not usually
      preserve determinants.
    </p>

    <p>
      Do not row reduce <m>A</m> alone if the system is
      <md>A\mathbf x=\mathbf b.</md>
      Row reduce the augmented matrix
      <md>[A\mid \mathbf b].</md>
    </p>

    <p>Free variables are not errors. They describe infinitely many solutions.</p>
  </warning>
</section>
