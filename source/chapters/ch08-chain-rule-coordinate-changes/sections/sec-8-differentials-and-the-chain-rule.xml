<?xml version="1.0" encoding="UTF-8"?>
<!-- Converted from multivariable_calculus/chapters/ch08-chain-rule-coordinate-changes.tex, Section 3.
     Status: CONVERTED. -->
<section xml:id="sec-8-differentials-and-the-chain-rule" xmlns:xi="http://www.w3.org/2001/XInclude">
  <title>Differentials and the chain rule</title>

  <p>
    <xref ref="sec-8-the-matrix-chain-rule"/> wrote the chain rule as a matrix product:
    <md>J_{F\circ G}=J_FJ_G.</md>
    That is the right language for transformations. But
    <xref ref="sec-7-differentials"/> gave us another language for the same linear idea:
    <md>df=f_x\,dx+f_y\,dy.</md>
    The chain rule becomes especially clean when we substitute one differential into another.
  </p>

  <subsection xml:id="c8s3-subsec-substitution-into-differentials">
    <title>Substitution into differentials</title>

    <p>
      Return to the greenhouse temperature field from <xref ref="sec-8-the-chain-rule-along-a-curve"/>:
      <md>T(x,y)=20+3x-y-\frac12x^2-\frac14y^2.</md>
      Its differential is
      <md>dT=(3-x)\,dx+\left(-1-\frac12y\right)\,dy.</md>
    </p>

    <p>
      Now suppose the greenhouse floor is being described by a slanted coordinate grid
      <m>(u,v)</m>, where
      <md>x=1+u+2v, \qquad y=2+u-v.</md>
      The point <m>(u,v)=(0,0)</m> corresponds to
      <md>(x,y)=(1,2).</md>
      The temperature as a function of the grid coordinates is
      <md>A(u,v)=T(1+u+2v,\;2+u-v).</md>
    </p>

    <p>
      We could expand <m>A(u,v)</m> and differentiate. But the differential does the same work
      without expanding.
    </p>

    <p>
      First,
      <md>dx=d(1+u+2v)=du+2\,dv,</md>
      and
      <md>dy=d(2+u-v)=du-dv.</md>
      At <m>(x,y)=(1,2)</m>,
      <md>dT_{(1,2)} = 2\,dx-2\,dy.</md>
      Substitute the expressions for <m>dx</m> and <m>dy</m>:
      <md>
        <mrow>dA_{(0,0)} \amp= 2(du+2\,dv)-2(du-dv)</mrow>
        <mrow>\amp= 6\,dv.</mrow>
      </md>
      So, at the center point of this grid,
      <md>A_u(0,0)=0, \qquad A_v(0,0)=6.</md>
      A tiny step in the <m>u</m>-direction does not change temperature to first order. A tiny
      step in the <m>v</m>-direction raises the temperature at rate <m>6</m>.
    </p>

    <p>
      The same computation works away from the point. Since
      <md>dT=(3-x)\,dx+\left(-1-\frac12y\right)\,dy,</md>
      and
      <md>dx=du+2\,dv, \qquad dy=du-dv,</md>
      we get
      <md>
        <mrow>dA \amp= (3-x)(du+2\,dv) + \left(-1-\frac12y\right)(du-dv)</mrow>
        <mrow>\amp= \left(2-x-\frac12y\right)du + \left(7-2x+\frac12y\right)dv,</mrow>
      </md>
      where
      <md>x=1+u+2v, \qquad y=2+u-v.</md>
      Thus
      <md>A_u= 2-x-\frac12y, \qquad A_v= 7-2x+\frac12y.</md>
      This is the chain rule written as substitution into differentials.
    </p>
  </subsection>

  <subsection xml:id="c8s3-subsec-general-substitution-rule">
    <title>The general substitution rule</title>

    <p>
      Let
      <md>z=f(x,y),</md>
      where
      <md>x=x(u,v), \qquad y=y(u,v).</md>
      Then
      <md>z=f(x(u,v),y(u,v)).</md>
      The differential of <m>f</m> is
      <md>df=f_x\,dx+f_y\,dy.</md>
      The intermediate coordinates have differentials
      <md>dx=x_u\,du+x_v\,dv,</md>
      and
      <md>dy=y_u\,du+y_v\,dv.</md>
      Substitute:
      <md>
        <mrow>dz \amp= f_x(x_u\,du+x_v\,dv) + f_y(y_u\,du+y_v\,dv)</mrow>
        <mrow>\amp= (f_xx_u+f_yy_u)\,du + (f_xx_v+f_yy_v)\,dv.</mrow>
      </md>
      Therefore
      <md>\boxed{ \frac{\partial z}{\partial u} = f_x\frac{\partial x}{\partial u} + f_y\frac{\partial y}{\partial u}, }</md>
      and
      <md>\boxed{ \frac{\partial z}{\partial v} = f_x\frac{\partial x}{\partial v} + f_y\frac{\partial y}{\partial v}. }</md>
      The partial derivatives <m>f_x</m> and <m>f_y</m> are evaluated at
      <md>(x(u,v),y(u,v)).</md>
    </p>

    <p>
      This is the same chain rule as <xref ref="sec-8-the-matrix-chain-rule"/>. In matrix form,
      <md>
        \begin{pmatrix}
            z_u \amp z_v
        \end{pmatrix}
        =
        \begin{pmatrix}
            f_x \amp f_y
        \end{pmatrix}
        \begin{pmatrix}
            x_u \amp x_v\\
            y_u \amp y_v
        \end{pmatrix}.
      </md>
      In differential form,
      <md>dz=f_x\,dx+f_y\,dy,</md>
      with
      <md>dx=x_u\,du+x_v\,dv, \qquad dy=y_u\,du+y_v\,dv.</md>
      The two statements are the same linear map, written in two dialects.
    </p>

    <theorem xml:id="c8s3-thm-chain-rule-for-differentials">
      <title>Chain rule for differentials</title>
      <statement>
        <p>
          Let
          <md>G:U\subseteq\mathbb R^k\to\mathbb R^n</md>
          be differentiable at <m>\mathbf p</m>, and let
          <md>f:V\subseteq\mathbb R^n\to\mathbb R</md>
          be differentiable at <m>G(\mathbf p)</m>, with <m>G(U)\subseteq V</m>. Then
          <md>f\circ G</md>
          is differentiable at <m>\mathbf p</m>, and
          <md>\boxed{ d(f\circ G)_{\mathbf p} = df_{G(\mathbf p)}\circ DG(\mathbf p). }</md>
          Equivalently, for every small input displacement <m>\mathbf h</m>,
          <md>\boxed{ d(f\circ G)_{\mathbf p}(\mathbf h) = df_{G(\mathbf p)}\bigl(DG(\mathbf p)\mathbf h\bigr). }</md>
        </p>
      </statement>
      <proof>
        <p>
          This is the matrix chain rule from <xref ref="sec-8-the-matrix-chain-rule"/>, with the
          outer map scalar-valued. The derivative of <m>G</m> sends an input displacement
          <m>\mathbf h</m> to the predicted displacement in the intermediate space. The
          differential <m>df</m> then eats that predicted intermediate displacement and returns
          the predicted change in <m>f</m>. Thus the first-order change of the composition is
          <md>df_{G(\mathbf p)}\circ DG(\mathbf p).</md>
        </p>
      </proof>
    </theorem>
  </subsection>

  <subsection xml:id="c8s3-subsec-why-du-behaves-naturally">
    <title>Why <m>du=u_x\,dx+u_y\,dy</m> behaves naturally</title>

    <p>
      The notation
      <md>du=u_x\,dx+u_y\,dy</md>
      can look suspicious the first time we see it. It helps to remember what <m>du</m> is:
      the differential of the function <m>u(x,y)</m>.
    </p>

    <p>
      For example, define new coordinate readings
      <md>u=x+2y, \qquad v=x-y.</md>
      Then
      <md>du=dx+2\,dy,</md>
      and
      <md>dv=dx-dy.</md>
      These equations say how the new coordinate readings change when the old point moves.
    </p>

    <p>
      Now solve for <m>x</m> and <m>y</m>:
      <md>x=\frac{u+2v}{3}, \qquad y=\frac{u-v}{3}.</md>
      So
      <md>dx=\frac13\,du+\frac23\,dv,</md>
      and
      <md>dy=\frac13\,du-\frac13\,dv.</md>
      Substitute these into the formula for <m>du</m>:
      <md>
        <mrow>dx+2\,dy \amp= \left(\frac13\,du+\frac23\,dv\right) + 2\left(\frac13\,du-\frac13\,dv\right)</mrow>
        <mrow>\amp= du.</mrow>
      </md>
      The notation checks itself.
    </p>

    <p>
      The reason is not algebraic luck. The symbol <m>d</m> means <q>take the linear part.</q>
      Linear parts compose cleanly, so their coordinate formulas substitute cleanly.
    </p>
  </subsection>

  <subsection xml:id="c8s3-subsec-pullback-preview">
    <title>Pullback preview: moving formulas to parameter space</title>

    <p>
      A <m>1</m>-form in the plane has the shape
      <md>\omega=P(x,y)\,dx+Q(x,y)\,dy.</md>
      A parametrization or coordinate change
      <md>G(u,v)=(x(u,v),y(u,v))</md>
      moves this form to the <m>(u,v)</m>-plane by substituting:
      <md>\boxed{ G^*\omega = P(x(u,v),y(u,v))\,d(x(u,v)) + Q(x(u,v),y(u,v))\,d(y(u,v)). }</md>
      This new <m>1</m>-form
      <md>G^*\omega</md>
      is called the <term>pullback</term> of <m>\omega</m> by <m>G</m>.
    </p>

    <p>
      The name sounds abstract, but the action is concrete: replace <m>x,y,dx,dy</m> by their
      expressions in the new variables.
    </p>

    <example xml:id="c8s3-ex-pulling-swirl-into-polar">
      <title>Pulling the swirl form into polar coordinates</title>
      <statement>
        <p>
          Consider the swirling <m>1</m>-form
          <md>\omega=-y\,dx+x\,dy.</md>
          Use the polar coordinate map
          <md>G(r,\theta)=(r\cos\theta,r\sin\theta).</md>
          Then
          <md>x=r\cos\theta, \qquad y=r\sin\theta.</md>
          Also,
          <md>dx=\cos\theta\,dr-r\sin\theta\,d\theta,</md>
          and
          <md>dy=\sin\theta\,dr+r\cos\theta\,d\theta.</md>
          Substitute into <m>\omega</m>:
          <md>
            <mrow>G^*\omega \amp= -r\sin\theta(\cos\theta\,dr-r\sin\theta\,d\theta) + r\cos\theta(\sin\theta\,dr+r\cos\theta\,d\theta)</mrow>
            <mrow>\amp= (-r\sin\theta\cos\theta+r\cos\theta\sin\theta)\,dr</mrow>
            <mrow>\amp\qquad + r^2(\sin^2\theta+\cos^2\theta)\,d\theta</mrow>
            <mrow>\amp= r^2\,d\theta.</mrow>
          </md>
          So
          <md>\boxed{ G^*(-y\,dx+x\,dy)=r^2\,d\theta. }</md>
          The trigonometry disappeared. In polar coordinates, the swirl form simply measures
          angular motion, weighted by <m>r^2</m>.
        </p>
      </statement>
    </example>

    <p>
      This is the first real glimpse of why differential forms fit coordinate changes so well.
      They are built to be substituted into.
    </p>
  </subsection>

  <subsection xml:id="c8s3-subsec-when-substitution-breaks">
    <title>When substitution breaks</title>

    <p>
      The differentiability condition is not cosmetic. If the coordinate map has a corner,
      there may be no linear part to substitute.
    </p>

    <p>
      Let
      <md>G(u)=(|u|,0),</md>
      and take the <m>1</m>-form
      <md>\omega=dx.</md>
      For <m>u&gt;0</m>,
      <md>x=|u|=u,</md>
      so
      <md>G^*\omega=du.</md>
      For <m>u&lt;0</m>,
      <md>x=|u|=-u,</md>
      so
      <md>G^*\omega=-du.</md>
      At <m>u=0</m>, the function
      <md>|u|</md>
      has no derivative. Therefore <m>d|u|</m> does not exist at <m>0</m>, and the pullback of
      <m>dx</m> is not defined there.
    </p>

    <p>
      This is the same warning as the chain rule along a curve:
      <md>\text{no differentiability} \quad\Longrightarrow\quad \text{no dependable first-order substitution.}</md>
    </p>

    <p>
      The outer function also has to be differentiable. If we try to substitute a fake
      <q>differential</q> made only from partial derivatives at a point where no total
      derivative exists, the same counterexamples from
      <xref ref="sec-8-the-chain-rule-along-a-curve"/> and
      <xref ref="sec-8-the-matrix-chain-rule"/> return. The differential is the total
      derivative, not a list of coordinate slopes.
    </p>
  </subsection>

  <subsection xml:id="c8s3-subsec-coordinate-free-notation">
    <title>A first look at coordinate-free notation</title>

    <p>
      The pullback formula lets us write the chain rule in a short form:
      <md>\boxed{ d(f\circ G)=G^*(df). }</md>
      Read it from right to left.
    </p>

    <p>
      First take the differential of <m>f</m> in the <m>x</m>-space:
      <md>df.</md>
      Then pull that differential back through the map <m>G</m> into the input space:
      <md>G^*(df).</md>
      The result is the differential of the composite:
      <md>d(f\circ G).</md>
    </p>

    <p>
      This notation hides the coordinates, but it does not hide the computation. If
      <md>G(u,v)=(x(u,v),y(u,v)),</md>
      then
      <md>G^*(df) = f_x(x(u,v),y(u,v))\,dx + f_y(x(u,v),y(u,v))\,dy,</md>
      where
      <md>dx=x_u\,du+x_v\,dv, \qquad dy=y_u\,du+y_v\,dv.</md>
      Expanding gives the familiar chain rule.
    </p>

    <p>
      The notation
      <md>d(f\circ G)=G^*(df)</md>
      says the same thing without committing to the names of the coordinates. That will
      matter more and more as we integrate forms over curves and surfaces.
    </p>

    <p>
      The pullback of the swirl form by polar coordinates gave
      <md>-y\,dx+x\,dy \quad\longmapsto\quad r^2\,d\theta.</md>
      That little simplification is a hint. Some problems are hard only because the coordinates
      are poorly matched to the geometry. Polar, cylindrical, and spherical coordinates are
      not new calculus rules. They are coordinate changes, and the chain rule tells us exactly
      how derivatives and differentials move through them.
    </p>
  </subsection>
</section>
