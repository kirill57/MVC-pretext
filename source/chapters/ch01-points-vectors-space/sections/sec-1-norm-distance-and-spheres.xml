<?xml version="1.0" encoding="UTF-8"?>
<!-- Converted from multivariable_calculus/chapters/ch01-points-vectors-space.tex, Section 5.
     Status: CONVERTED. -->
<section xml:id="sec-1-norm-distance-and-spheres" xmlns:xi="http://www.w3.org/2001/XInclude">
  <title>Norm, distance, and spheres</title>

  <p>
    The last section ended with a simple problem. We know how to describe directions with
    vectors, and we know when directions are redundant. But we still do not know how long a
    vector is.
  </p>

  <p>
    That question sounds small. It is not. Length is what turns a displacement into a distance,
    a velocity vector into a speed, and a direction into a unit direction.
  </p>

  <subsection xml:id="subsec-length-of-a-vector">
    <title>Length of a vector</title>

    <p>
      A drone receives the command
      <md>\mathbf v=(6,2,3).</md>
      The command means:
      <md>6 \text{ meters east},\qquad 2 \text{ meters north},\qquad 3 \text{ meters up}.</md>
      How far is the drone from where it started?
    </p>

    <p>
      First ignore the upward motion. On the floor, the drone moves
      <md>(6,2),</md>
      whose length is
      <md>\sqrt{6^2+2^2}=\sqrt{40}.</md>
      Now add the upward motion. The floor displacement <m>\sqrt{40}</m> and the height
      <m>3</m> make another right triangle, so the total length is
      <md>\sqrt{(\sqrt{40})^2+3^2}=\sqrt{40+9}=7.</md>
      The vector <m>(6,2,3)</m> has length <m>7</m>.
    </p>

    <definition xml:id="def-norm">
      <title>Norm</title>
      <statement>
        <p>
          The <term>norm</term> of a vector
          <md>\mathbf v=(v_1,v_2,\ldots,v_n)</md>
          in <m>\mathbb R^n</m> is
          <md>\|\mathbf v\|=\sqrt{v_1^2+v_2^2+\cdots+v_n^2}.</md>
          The norm is also called the <term>length</term> or <term>magnitude</term> of the
          vector.
        </p>
      </statement>
    </definition>

    <p>
      In the plane,
      <md>\|(a,b)\|=\sqrt{a^2+b^2}.</md>
      In space,
      <md>\|(a,b,c)\|=\sqrt{a^2+b^2+c^2}.</md>
    </p>

    <example xml:id="ex-familiar-length">
      <title>A familiar length</title>
      <statement>
        <p>
          The vector
          <md>\mathbf v=(3,4)</md>
          has norm
          <md>\|\mathbf v\|=\sqrt{3^2+4^2}=5.</md>
          So the old <m>3</m>-<m>4</m>-<m>5</m> right triangle is now a vector length
          calculation.
        </p>
      </statement>
    </example>

    <example xml:id="ex-space-displacement-norm">
      <title>A space displacement</title>
      <statement>
        <p>
          The vector
          <md>\mathbf w=(2,-1,2)</md>
          has norm
          <md>\|\mathbf w\|=\sqrt{2^2+(-1)^2+2^2}=\sqrt{9}=3.</md>
          The negative sign does not make the vector shorter or longer. It only says that the
          motion in that coordinate direction is opposite to the positive direction.
        </p>
      </statement>
    </example>

    <figure xml:id="fig-norm-triangle">
      <caption>The norm is the distance from the tail of the vector to its head.</caption>
      <image width="55%">
        <latex-image>
        \begin{tikzpicture}[scale=0.9, &gt;=stealth]
          \draw[-&gt;] (-0.3,0) -- (4.6,0) node[right] {\(x\)};
          \draw[-&gt;] (0,-0.3) -- (0,3.4) node[above] {\(y\)};
          \draw[-&gt;, very thick] (0,0) -- (3,2) node[midway, above left] {\(\mathbf v=(3,2)\)};
          \draw[dashed] (3,0) -- (3,2);
          \draw[dashed] (0,2) -- (3,2);
          \node[below] at (3,0) {\(3\)};
          \node[left] at (0,2) {\(2\)};
          \node[below left] at (0,0) {\(0\)};
          \node[right] at (3.1,1.1) {\(\|\mathbf v\|=\sqrt{3^2+2^2}\)};
        \end{tikzpicture}
        </latex-image>
      </image>
    </figure>

    <p>
      The norm of the zero vector is
      <md>\|\mathbf 0\|=0.</md>
      Every other vector has positive norm.
    </p>
  </subsection>

  <subsection xml:id="subsec-distance-between-points">
    <title>Distance between points</title>

    <p>A vector has length. Two points have distance between them.</p>

    <p>
      Suppose a ranger station is at
      <md>P=(2,1)</md>
      on a map, and a campfire is reported at
      <md>Q=(8,9).</md>
      The displacement from <m>P</m> to <m>Q</m> is
      <md>Q-P=(8-2,\;9-1)=(6,8).</md>
      Therefore the distance from <m>P</m> to <m>Q</m> is
      <md>\|Q-P\|=\|(6,8)\|=\sqrt{6^2+8^2}=10.</md>
    </p>

    <definition xml:id="def-distance">
      <title>Distance</title>
      <statement>
        <p>
          The distance between points
          <md>P=(p_1,p_2,\ldots,p_n)</md>
          and
          <md>Q=(q_1,q_2,\ldots,q_n)</md>
          in <m>\mathbb R^n</m> is
          <md>d(P,Q)=\|Q-P\|=\sqrt{(q_1-p_1)^2+(q_2-p_2)^2+\cdots+(q_n-p_n)^2}.</md>
        </p>
      </statement>
    </definition>

    <p>
      The order of the points does not matter:
      <md>d(P,Q)=d(Q,P).</md>
      Indeed,
      <md>Q-P=-(P-Q),</md>
      and a vector and its negative have the same length.
    </p>

    <example xml:id="ex-distance-in-space">
      <title>Distance in space</title>
      <statement>
        <p>
          Let
          <md>P=(1,2,0),\qquad Q=(4,-2,12).</md>
          Then
          <md>Q-P=(3,-4,12),</md>
          so
          <md>d(P,Q)=\sqrt{3^2+(-4)^2+12^2}=\sqrt{9+16+144}=13.</md>
        </p>
      </statement>
    </example>

    <p>Notice the pattern:</p>

    <table xml:id="tab-length-vs-distance">
      <title>Length of a vector versus distance between points.</title>
      <tabular>
        <row header="yes" bottom="medium">
          <cell>Object</cell>
          <cell>What we measure</cell>
        </row>
        <row><cell><m>\mathbf v</m></cell><cell><m>\|\mathbf v\|</m></cell></row>
        <row><cell><m>P,Q</m></cell><cell><m>d(P,Q)=\|Q-P\|</m></cell></row>
      </tabular>
    </table>

    <p>
      A vector has a length by itself. Two points have a distance because their difference is a
      vector.
    </p>
  </subsection>

  <subsection xml:id="subsec-circles-spheres">
    <title>Circles, spheres, and higher-dimensional spheres</title>

    <p>
      A radio transmitter sits at
      <md>C=(3,2)</md>
      on a flat map. Its signal reaches every point within <m>5</m> km of <m>C</m>. The edge of
      the coverage region is the set of points <m>(x,y)</m> whose distance from <m>C</m> is
      <m>5</m>:
      <md>d((x,y),C)=5.</md>
      Using the distance formula,
      <md>\sqrt{(x-3)^2+(y-2)^2}=5.</md>
      Squaring both sides gives
      <md>(x-3)^2+(y-2)^2=25.</md>
      This is a circle.
    </p>

    <p>
      The filled-in coverage region is
      <md>(x-3)^2+(y-2)^2\leq 25.</md>
      The boundary is a circle; the filled-in region is a disk.
    </p>

    <figure xml:id="fig-circle-center">
      <caption>A circle is the set of points at a fixed distance from a center.</caption>
      <image width="55%">
        <latex-image>
        \begin{tikzpicture}[scale=0.75, &gt;=stealth]
          \draw[-&gt;] (-2,0) -- (9,0) node[right] {\(x\)};
          \draw[-&gt;] (0,-3) -- (0,8) node[above] {\(y\)};
          \draw[thick] (3,2) circle (3.2);
          \fill (3,2) circle (2pt);
          \node[below left] at (3,2) {\(C=(3,2)\)};
          \draw[-&gt;, thick] (3,2) -- (6.2,2);
          \node[above] at (4.7,2) {\(5\)};
          \node at (3,-2.0) {\((x-3)^2+(y-2)^2=25\)};
        \end{tikzpicture}
        </latex-image>
      </image>
    </figure>

    <p>
      The same idea works in space. A point <m>(x,y,z)</m> is on the sphere of radius <m>5</m>
      centered at
      <md>C=(3,2,1)</md>
      when
      <md>d((x,y,z),C)=5.</md>
      Thus
      <md>(x-3)^2+(y-2)^2+(z-1)^2=25.</md>
    </p>

    <definition xml:id="def-spheres-balls">
      <title>Spheres and balls</title>
      <statement>
        <p>
          In <m>\mathbb R^n</m>, the sphere of radius <m>r\geq 0</m> centered at
          <md>C=(c_1,c_2,\ldots,c_n)</md>
          is the set of points <m>X=(x_1,x_2,\ldots,x_n)</m> satisfying
          <md>\|X-C\|=r.</md>
          Equivalently,
          <md>(x_1-c_1)^2+(x_2-c_2)^2+\cdots+(x_n-c_n)^2=r^2.</md>
        </p>
        <p>
          The ball of radius <m>r</m> centered at <m>C</m> is the set of points satisfying
          <md>\|X-C\|\leq r.</md>
        </p>
      </statement>
    </definition>

    <p>
      In <m>\mathbb R^2</m>, a sphere is what we usually call a circle. In <m>\mathbb R^3</m>, a
      sphere is the surface of a round ball. In <m>\mathbb R^4</m> and beyond, we cannot draw
      the sphere honestly, but the equation still makes perfect sense.
    </p>

    <p>
      The condition <m>r\geq 0</m> is not decoration. Distance is never negative. If
      <m>r&lt;0</m>, then
      <md>\|X-C\|=r</md>
      has no solutions. If <m>r=0</m>, the sphere is just the single point <m>C</m>.
    </p>
  </subsection>

  <subsection xml:id="subsec-triangle-inequality">
    <title>The triangle inequality and why it matters</title>

    <p>
      A hiker walks from a cabin to a lake, then from the lake to a lookout point. The direct
      path from the cabin to the lookout cannot be longer than the broken path through the lake.
      This everyday fact becomes one of the most useful inequalities in calculus.
    </p>

    <p>
      For example, let
      <md>\mathbf u=(3,1),\qquad \mathbf v=(1,2).</md>
      Then
      <md>\mathbf u+\mathbf v=(4,3).</md>
      The lengths are
      <md>\|\mathbf u\|=\sqrt{10},\qquad \|\mathbf v\|=\sqrt{5},\qquad \|\mathbf u+\mathbf v\|=5.</md>
      The direct vector <m>\mathbf u+\mathbf v</m> is shorter than taking <m>\mathbf u</m> and
      then <m>\mathbf v</m>:
      <md>5 \leq \sqrt{10}+\sqrt{5}.</md>
    </p>

    <theorem xml:id="thm-triangle-inequality">
      <title>Triangle inequality</title>
      <statement>
        <p>
          For any vectors <m>\mathbf u,\mathbf v\in\mathbb R^n</m>,
          <md>\|\mathbf u+\mathbf v\|\leq \|\mathbf u\|+\|\mathbf v\|.</md>
          Equivalently, for any three points <m>P,Q,R\in\mathbb R^n</m>,
          <md>d(P,R)\leq d(P,Q)+d(Q,R).</md>
        </p>
      </statement>
      <proof>
        <title>Why this is true geometrically</title>
        <p>
          Draw <m>\mathbf u</m> first, then draw <m>\mathbf v</m> starting at the head of
          <m>\mathbf u</m>. The vector <m>\mathbf u+\mathbf v</m> goes directly from the first
          tail to the last head. These three segments form a triangle, possibly flattened into
          a line. The direct side of a triangle is never longer than the other two sides
          together.
        </p>
        <p>
          A fully algebraic proof will come naturally after the next chapter gives a name to the
          expression
          <md>u_1v_1+u_2v_2+\cdots+u_nv_n.</md>
          That name is the <term>dot product</term>.
        </p>
      </proof>
    </theorem>

    <p>
      Equality happens when the two vectors point in the same direction. For example,
      <md>\mathbf u=(2,1),\qquad \mathbf v=(6,3)=3\mathbf u.</md>
      Then
      <md>\|\mathbf u+\mathbf v\|=\|(8,4)\|=4\sqrt{5},</md>
      while
      <md>\|\mathbf u\|+\|\mathbf v\|=\sqrt5+3\sqrt5=4\sqrt5.</md>
      The broken path is not really broken; both pieces lie along the same ray.
    </p>

    <p>
      The triangle inequality will return often. When we later say that a function is
      continuous, or that a curve stays close to another curve, the triangle inequality is the
      tool that lets us control a complicated distance by breaking it into simpler pieces.
    </p>
  </subsection>

  <subsection xml:id="subsec-unit-vectors-normalization">
    <title>Unit vectors and normalization</title>

    <p>Sometimes length is not what we care about. We only want direction.</p>

    <p>
      A wind sensor reports the displacement direction
      <md>\mathbf v=(6,2).</md>
      Its length is
      <md>\|\mathbf v\|=\sqrt{6^2+2^2}=\sqrt{40}=2\sqrt{10}.</md>
      If we divide <m>\mathbf v</m> by its own length, we get
      <md>\frac{\mathbf v}{\|\mathbf v\|}=\frac{(6,2)}{2\sqrt{10}}=\left(\frac{3}{\sqrt{10}},\frac{1}{\sqrt{10}}\right).</md>
      This new vector points in the same direction as <m>\mathbf v</m>, but has length
      <m>1</m>.
    </p>

    <definition xml:id="def-unit-vector-normalization">
      <title>Unit vector and normalization</title>
      <statement>
        <p>
          A vector <m>\mathbf u</m> is a <term>unit vector</term> if
          <md>\|\mathbf u\|=1.</md>
          If <m>\mathbf v\neq \mathbf 0</m>, the unit vector in the direction of
          <m>\mathbf v</m> is
          <md>\widehat{\mathbf v}=\frac{\mathbf v}{\|\mathbf v\|}.</md>
          This process is called <term>normalizing</term> the vector.
        </p>
      </statement>
    </definition>

    <p>
      The condition <m>\mathbf v\neq \mathbf 0</m> matters. The formula
      <md>\frac{\mathbf v}{\|\mathbf v\|}</md>
      would require division by <m>0</m> if <m>\mathbf v=\mathbf 0</m>. But the deeper problem
      is geometric: the zero vector has no direction. There is no unit vector pointing in the
      same direction as no direction.
    </p>

    <example xml:id="ex-speed-and-direction">
      <title>Speed and direction</title>
      <statement>
        <p>
          A small boat has velocity
          <md>\mathbf v=(12,5)</md>
          measured in meters per second. Its speed is
          <md>\|\mathbf v\|=\sqrt{12^2+5^2}=13.</md>
          Its direction is the unit vector
          <md>\widehat{\mathbf v}=\left(\frac{12}{13},\frac{5}{13}\right).</md>
          So the velocity can be written as
          <md>\mathbf v=13\left(\frac{12}{13},\frac{5}{13}\right).</md>
          This separates the motion into
          <md>\text{speed}\times\text{direction}.</md>
        </p>
      </statement>
    </example>

    <p>
      Now that length and distance are available, our earlier descriptions of lines and planes
      become sharper. A road is not just a set of points of the form <m>P+t\mathbf u</m>; we can
      ask how far a house is from that road. A wall is not just a flat surface; we can ask for
      the shortest distance from a sensor to the wall. Those questions lead us back to lines and
      planes, but now with measurement attached.
    </p>
  </subsection>
</section>
