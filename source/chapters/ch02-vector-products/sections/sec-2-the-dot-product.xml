<?xml version="1.0" encoding="UTF-8"?>
<!-- Converted from multivariable_calculus/chapters/ch02-vector-products.tex, Section "The dot product".
     Status: CONVERTED. -->
<section xml:id="sec-2-the-dot-product" xmlns:xi="http://www.w3.org/2001/XInclude">
  <title>The dot product</title>

  <p>
    The last section pulled the dot product out of the scalar part of quaternion
    multiplication:
    <md>\operatorname{Scal}(UV)=-\mathbf u\cdot\mathbf v.</md>
    Now we study the dot product on its own. It is the simplest way to ask a geometric
    question:
    <md>\text{How much does one vector point along another?}</md>
  </p>

  <subsection xml:id="c2s4-algebraic-definition-and-first-examples">
    <title>Algebraic definition and first examples</title>

    <p>
      Imagine dragging a sled across flat snow. The sled moves
      <md>\mathbf d=(3,4)</md>
      meters: <m>3</m> meters east and <m>4</m> meters north.
    </p>

    <p>
      First suppose the pull is purely east:
      <md>\mathbf F=(10,0).</md>
      Only the east part of the motion helps the pull do work. The northward motion is
      sideways to the force. So the useful multiplication is
      <md>10\cdot3+0\cdot4=30.</md>
    </p>

    <p>
      Now suppose the force is
      <md>\mathbf F=(6,8).</md>
      Then both components help:
      <md>6\cdot3+8\cdot4=18+32=50.</md>
    </p>

    <p>
      Finally suppose the force is
      <md>\mathbf F=(-4,3).</md>
      Then
      <md>(-4)\cdot3+3\cdot4=-12+12=0.</md>
      This force is perpendicular to the displacement. It pulls sideways, so it does no work
      along that motion.
    </p>

    <p>
      The same component-by-component multiplication is the dot product.
    </p>

    <definition xml:id="c2s4-def-dot-product">
      <title>Dot product</title>
      <statement>
        <p>
          For vectors in the plane,
          <md>\mathbf u=(u_1,u_2),\qquad \mathbf v=(v_1,v_2),</md>
          the <term>dot product</term> is
          <md>\mathbf u\cdot\mathbf v=u_1v_1+u_2v_2.</md>
        </p>
        <p>
          For vectors in space,
          <md>\mathbf u=(u_1,u_2,u_3),\qquad \mathbf v=(v_1,v_2,v_3),</md>
          the <term>dot product</term> is
          <md>\mathbf u\cdot\mathbf v=u_1v_1+u_2v_2+u_3v_3.</md>
        </p>
      </statement>
    </definition>

    <example xml:id="c2s4-ex-dot-product-in-space">
      <title>A dot product in space</title>
      <statement>
        <p>
          Let
          <md>\mathbf u=(2,-1,3),\qquad \mathbf v=(4,5,-2).</md>
          Then
          <md>
            <mrow>\mathbf u\cdot\mathbf v \amp= 2\cdot4+(-1)\cdot5+3\cdot(-2)</mrow>
            <mrow>\amp= 8-5-6</mrow>
            <mrow>\amp= -3.</mrow>
          </md>
          The negative answer tells us that the two vectors point somewhat against each other.
        </p>
      </statement>
    </example>

    <p>
      The dot product behaves like multiplication in several useful ways.
    </p>

    <theorem xml:id="c2s4-thm-basic-algebra-dot-product">
      <title>Basic algebra of the dot product</title>
      <statement>
        <p>
          For vectors <m>\mathbf u,\mathbf v,\mathbf w</m> and a scalar <m>c</m>,
          <md>\mathbf u\cdot\mathbf v=\mathbf v\cdot\mathbf u,</md>
          <md>\mathbf u\cdot(\mathbf v+\mathbf w)=\mathbf u\cdot\mathbf v+\mathbf u\cdot\mathbf w,</md>
          and
          <md>(c\mathbf u)\cdot\mathbf v=c(\mathbf u\cdot\mathbf v).</md>
          Also,
          <md>\mathbf u\cdot\mathbf u=\|\mathbf u\|^2.</md>
        </p>
      </statement>
      <proof>
        <p>
          All four statements follow by writing the components and using ordinary arithmetic.
          For example, in space,
          <md>
            <mrow>\mathbf u\cdot(\mathbf v+\mathbf w) \amp= u_1(v_1+w_1)+u_2(v_2+w_2)+u_3(v_3+w_3)</mrow>
            <mrow>\amp= (u_1v_1+u_2v_2+u_3v_3)+(u_1w_1+u_2w_2+u_3w_3)</mrow>
            <mrow>\amp= \mathbf u\cdot\mathbf v+\mathbf u\cdot\mathbf w.</mrow>
          </md>
          And
          <md>\mathbf u\cdot\mathbf u=u_1^2+u_2^2+u_3^2=\|\mathbf u\|^2.</md>
        </p>
      </proof>
    </theorem>

    <p>
      That last identity is the first sign that the dot product knows about length:
      <md>\|\mathbf u\|=\sqrt{\mathbf u\cdot\mathbf u}.</md>
    </p>
  </subsection>

  <subsection xml:id="c2s4-length-angle-and-orthogonality">
    <title>Length, angle, and orthogonality</title>

    <p>
      The dot product does more than compute a number. It computes angles.
    </p>

    <p>
      Take
      <md>\mathbf u=(3,0),\qquad \mathbf v=(2,2\sqrt3).</md>
      Then
      <md>\|\mathbf u\|=3,</md>
      and
      <md>\|\mathbf v\|=\sqrt{2^2+(2\sqrt3)^2}=\sqrt{4+12}=4.</md>
      Their dot product is
      <md>\mathbf u\cdot\mathbf v=3\cdot2+0\cdot2\sqrt3=6.</md>
      So
      <md>\frac{\mathbf u\cdot\mathbf v}{\|\mathbf u\|\|\mathbf v\|}=\frac{6}{3\cdot4}=\frac12.</md>
      The angle whose cosine is <m>1/2</m> is <m>60^\circ</m>, or <m>\pi/3</m> radians. So these vectors
      meet at a <m>60^\circ</m> angle.
    </p>

    <figure xml:id="c2s4-fig-dot-product-angle">
      <caption>The dot product finds the angle through <m>\cos\theta</m>.</caption>
      <image width="60%">
        <latex-image>
        \begin{tikzpicture}[scale=0.8, &gt;=stealth]
            \draw[-&gt;] (-0.5,0) -- (5,0) node[right] {\(x\)};
            \draw[-&gt;] (0,-0.5) -- (0,4) node[above] {\(y\)};

            \draw[-&gt;, very thick] (0,0) -- (3,0) node[midway, below] {\(\mathbf u=(3,0)\)};
            \draw[-&gt;, very thick] (0,0) -- (2,3.464) node[midway, left] {\(\mathbf v=(2,2\sqrt3)\)};

            \draw (0.9,0) arc (0:60:0.9);
            \node at (1.25,0.45) {\(\theta\)};

            \fill (0,0) circle (2pt);
        \end{tikzpicture}
        </latex-image>
      </image>
    </figure>

    <p>
      The pattern is general.
    </p>

    <theorem xml:id="c2s4-thm-dot-product-and-angle">
      <title>Dot product and angle</title>
      <statement>
        <p>
          Let <m>\mathbf u</m> and <m>\mathbf v</m> be nonzero vectors in the plane or in space. If
          <m>\theta</m> is the angle between them, with
          <md>0\leq \theta\leq \pi,</md>
          then
          <md>\boxed{\mathbf u\cdot\mathbf v=\|\mathbf u\|\,\|\mathbf v\|\cos\theta.}</md>
          Equivalently,
          <md>\cos\theta=\frac{\mathbf u\cdot\mathbf v}{\|\mathbf u\|\,\|\mathbf v\|}.</md>
        </p>
      </statement>
      <proof>
        <p>
          Look at the triangle formed by the two vectors <m>\mathbf u</m>, <m>\mathbf v</m>, and
          <m>\mathbf u-\mathbf v</m>. By algebra,
          <md>
            <mrow>\|\mathbf u-\mathbf v\|^2 \amp= (\mathbf u-\mathbf v)\cdot(\mathbf u-\mathbf v)</mrow>
            <mrow>\amp= \mathbf u\cdot\mathbf u-2\mathbf u\cdot\mathbf v+\mathbf v\cdot\mathbf v</mrow>
            <mrow>\amp= \|\mathbf u\|^2+\|\mathbf v\|^2-2\mathbf u\cdot\mathbf v.</mrow>
          </md>
          By the Law of Cosines applied to the same triangle,
          <md>\|\mathbf u-\mathbf v\|^2=\|\mathbf u\|^2+\|\mathbf v\|^2-2\|\mathbf u\|\,\|\mathbf v\|\cos\theta.</md>
          Comparing the two formulas gives
          <md>\mathbf u\cdot\mathbf v=\|\mathbf u\|\,\|\mathbf v\|\cos\theta.</md>
        </p>
      </proof>
    </theorem>

    <p>
      We required both vectors to be nonzero. That condition is not cosmetic. The zero
      vector has length <m>0</m>, but it has no direction. So there is no well-defined angle
      between
      <md>\mathbf 0</md>
      and
      <md>(1,0,0).</md>
      The formula
      <md>\cos\theta=\frac{\mathbf u\cdot\mathbf v}{\|\mathbf u\|\,\|\mathbf v\|}</md>
      would also divide by <m>0</m>.
    </p>

    <p>
      The dot product still makes sense with the zero vector:
      <md>\mathbf 0\cdot\mathbf v=0.</md>
      But the angle interpretation needs two nonzero vectors.
    </p>

    <definition xml:id="c2s4-def-orthogonal-vectors">
      <title>Orthogonal vectors</title>
      <statement>
        <p>
          Two vectors <m>\mathbf u</m> and <m>\mathbf v</m> are called <term>orthogonal</term> if
          <md>\mathbf u\cdot\mathbf v=0.</md>
          When both vectors are nonzero, this means they are perpendicular.
        </p>
      </statement>
    </definition>

    <example xml:id="c2s4-ex-perpendicular-pair">
      <title>A perpendicular pair</title>
      <statement>
        <p>
          Let
          <md>\mathbf u=(3,4),\qquad \mathbf v=(-4,3).</md>
          Then
          <md>\mathbf u\cdot\mathbf v=3(-4)+4(3)=-12+12=0.</md>
          So the two vectors are perpendicular.
        </p>
      </statement>
    </example>

    <p>
      There is a useful trick hiding in that example. In the plane, if
      <md>\mathbf u=(a,b),</md>
      then
      <md>(-b,a)</md>
      is perpendicular to <m>\mathbf u</m>, because
      <md>(a,b)\cdot(-b,a)=-ab+ab=0.</md>
      This is the same <m>90^\circ</m> rotation we saw when multiplying a complex number by
      <m>i</m>.
    </p>
  </subsection>

  <subsection xml:id="c2s4-projection-onto-a-line">
    <title>Projection onto a line</title>

    <p>
      The dot product tells us how much one vector lies along another.
    </p>

    <p>
      Suppose a narrow trail runs in the direction
      <md>\mathbf u=(3,4).</md>
      A gust of wind pushes a hiker by the displacement
      <md>\mathbf v=(7,1).</md>
      Part of this displacement is along the trail. Part is sideways.
    </p>

    <p>
      The dot product finds the along-trail part:
      <md>\mathbf v\cdot\mathbf u=7\cdot3+1\cdot4=25.</md>
      Also,
      <md>\mathbf u\cdot\mathbf u=3^2+4^2=25.</md>
      So the coefficient of <m>\mathbf u</m> in the along-trail part is
      <md>\frac{\mathbf v\cdot\mathbf u}{\mathbf u\cdot\mathbf u}=\frac{25}{25}=1.</md>
      Thus the projection of <m>\mathbf v</m> onto the trail direction is
      <md>1\cdot \mathbf u=(3,4).</md>
      The sideways leftover is
      <md>\mathbf v-\mathbf u=(7,1)-(3,4)=(4,-3).</md>
      Check that it is perpendicular to the trail:
      <md>(4,-3)\cdot(3,4)=12-12=0.</md>
    </p>

    <figure xml:id="c2s4-fig-projection-onto-line">
      <caption>A vector splits into an along-line projection and a perpendicular leftover.</caption>
      <image width="60%">
        <latex-image>
        \begin{tikzpicture}[scale=0.75, &gt;=stealth]
            \draw[-&gt;] (-0.5,0) -- (8.5,0) node[right] {\(x\)};
            \draw[-&gt;] (0,-3.5) -- (0,5.5) node[above] {\(y\)};

            \coordinate (O) at (0,0);
            \coordinate (U) at (3,4);
            \coordinate (V) at (7,1);
            \coordinate (R) at (4,-3);

            \draw[thick, gray!70] (-1,-1.333) -- (4.2,5.6) node[above] {\(\operatorname{span}\{\mathbf u\}\)};

            \draw[-&gt;, very thick] (O) -- (V) node[midway, above right] {\(\mathbf v=(7,1)\)};
            \draw[-&gt;, very thick] (O) -- (U) node[midway, left] {\(\operatorname{proj}_{\mathbf u}\mathbf v=(3,4)\)};
            \draw[-&gt;, thick, dashed] (U) -- (V) node[midway, right] {\((4,-3)\)};

            \draw (2.76, 3.68) -- (3.08, 3.44) -- (3.32, 3.76);

            \fill (O) circle (2pt);
            \fill (U) circle (2pt);
            \fill (V) circle (2pt);
        \end{tikzpicture}
        </latex-image>
      </image>
    </figure>

    <definition xml:id="c2s4-def-projection-onto-line">
      <title>Projection onto a line</title>
      <statement>
        <p>
          Let <m>\mathbf u\neq \mathbf 0</m>. The <term>projection of <m>\mathbf v</m> onto the line
          spanned by <m>\mathbf u</m></term> is
          <md>\boxed{\operatorname{proj}_{\mathbf u}\mathbf v=\frac{\mathbf v\cdot\mathbf u}{\mathbf u\cdot\mathbf u}\,\mathbf u.}</md>
          The scalar component of <m>\mathbf v</m> in the direction of <m>\mathbf u</m> is
          <md>\frac{\mathbf v\cdot\mathbf u}{\|\mathbf u\|}.</md>
        </p>
      </statement>
    </definition>

    <p>
      The condition
      <md>\mathbf u\neq\mathbf 0</md>
      is necessary. The zero vector does not determine a line direction, and the formula
      would divide by
      <md>\mathbf u\cdot\mathbf u=0.</md>
      For example, asking for the projection of <m>(1,2)</m> onto the <q>direction</q>
      <md>(0,0)</md>
      has no geometric meaning. There is no direction to project onto.
    </p>

    <p>
      Projection is not only a formula. It is a closest approximation.
    </p>

    <theorem xml:id="c2s4-thm-projection-closest-vector">
      <title>Projection is the closest vector on the line</title>
      <statement>
        <p>
          Let <m>\mathbf u\neq\mathbf 0</m>, and let
          <md>\mathbf p=\operatorname{proj}_{\mathbf u}\mathbf v.</md>
          Then <m>\mathbf p</m> is the vector on the line
          <md>\operatorname{span}\{\mathbf u\}</md>
          closest to <m>\mathbf v</m>.
        </p>
      </statement>
      <proof>
        <p>
          The vector <m>\mathbf p</m> has the form
          <md>\mathbf p=c\mathbf u,\qquad c=\frac{\mathbf v\cdot\mathbf u}{\mathbf u\cdot\mathbf u}.</md>
          The leftover
          <md>\mathbf r=\mathbf v-\mathbf p</md>
          is perpendicular to <m>\mathbf u</m>, because
          <md>
            <mrow>\mathbf r\cdot\mathbf u \amp= (\mathbf v-c\mathbf u)\cdot\mathbf u</mrow>
            <mrow>\amp= \mathbf v\cdot\mathbf u-c(\mathbf u\cdot\mathbf u)</mrow>
            <mrow>\amp= \mathbf v\cdot\mathbf u-\frac{\mathbf v\cdot\mathbf u}{\mathbf u\cdot\mathbf u}(\mathbf u\cdot\mathbf u)</mrow>
            <mrow>\amp= 0.</mrow>
          </md>
          Now take any other vector <m>t\mathbf u</m> on the same line. Then
          <md>\mathbf v-t\mathbf u=(\mathbf v-\mathbf p)+(\mathbf p-t\mathbf u).</md>
          The first vector is perpendicular to the line, and the second vector lies on the line.
          So by the Pythagorean theorem,
          <md>\|\mathbf v-t\mathbf u\|^2=\|\mathbf v-\mathbf p\|^2+\|\mathbf p-t\mathbf u\|^2.</md>
          This is smallest when
          <md>\|\mathbf p-t\mathbf u\|^2=0,</md>
          which happens only when
          <md>t\mathbf u=\mathbf p.</md>
        </p>
      </proof>
    </theorem>

    <p>
      This theorem is the first glimpse of a large idea: dot products allow us to turn geometry
      into approximation. Later, the same idea will become least squares, tangent planes, and
      orthogonal projections in higher-dimensional spaces.
    </p>
  </subsection>

  <subsection xml:id="c2s4-work-as-force-times-displacement">
    <title>Work as force times displacement</title>

    <p>
      In single-variable physics, constant work is often introduced as
      <md>\text{work}=\text{force}\times\text{distance}.</md>
      That formula assumes the force points in the same direction as the motion.
    </p>

    <p>
      The dot product gives the correct formula when force and displacement may point in
      different directions.
    </p>

    <definition xml:id="c2s4-def-work-constant-force">
      <title>Work by a constant force</title>
      <statement>
        <p>
          If a constant force <m>\mathbf F</m> moves an object through displacement <m>\mathbf d</m>,
          then the work done by the force is
          <md>\boxed{W=\mathbf F\cdot\mathbf d.}</md>
        </p>
      </statement>
    </definition>

    <example xml:id="c2s4-ex-pushing-a-crate">
      <title>Pushing a crate</title>
      <statement>
        <p>
          A crate moves
          <md>\mathbf d=(4,0)</md>
          meters across a floor. A force
          <md>\mathbf F=(12,5)</md>
          newtons acts on it. The work done is
          <md>W=\mathbf F\cdot\mathbf d=12\cdot4+5\cdot0=48.</md>
          So the force does
          <md>48\ \text{joules}</md>
          of work.
        </p>
        <p>
          The vertical component <m>5</m> does not contribute to the work across the floor, because
          the displacement has no vertical component.
        </p>
      </statement>
    </example>

    <p>
      Using the angle formula for the dot product,
      <md>\mathbf F\cdot\mathbf d=\|\mathbf F\|\,\|\mathbf d\|\cos\theta.</md>
      So only the component of the force in the direction of motion contributes to the work.
    </p>

    <p>
      If the force is perpendicular to the displacement, then
      <md>\theta=\frac{\pi}{2},\qquad \cos\theta=0,</md>
      and the work is zero.
    </p>

    <p>
      If the force points against the displacement, then
      <md>\cos\theta&lt;0,</md>
      and the work is negative. This is what friction does.
    </p>
  </subsection>

  <subsection xml:id="c2s4-cauchy-schwarz-and-geometry-of-approximation">
    <title>Cauchy–Schwarz and the geometry of approximation</title>

    <p>
      A projection cannot be longer than the vector being projected.
    </p>

    <p>
      For instance, if <m>\widehat{\mathbf u}</m> is a unit vector, then
      <md>\mathbf v\cdot\widehat{\mathbf u}</md>
      is the signed length of the shadow of <m>\mathbf v</m> in the direction
      <m>\widehat{\mathbf u}</m>. A shadow on a line cannot be longer than the object casting
      it:
      <md>|\mathbf v\cdot\widehat{\mathbf u}|\leq \|\mathbf v\|.</md>
      If <m>\mathbf u</m> is not a unit vector, the same idea becomes
      <md>|\mathbf u\cdot\mathbf v|\leq \|\mathbf u\|\,\|\mathbf v\|.</md>
    </p>

    <theorem xml:id="c2s4-thm-cauchy-schwarz">
      <title>Cauchy–Schwarz inequality</title>
      <statement>
        <p>
          For vectors <m>\mathbf u</m> and <m>\mathbf v</m> in the plane or in space,
          <md>\boxed{|\mathbf u\cdot\mathbf v|\leq \|\mathbf u\|\,\|\mathbf v\|.}</md>
          If both vectors are nonzero, equality holds exactly when the two vectors lie on the same
          line through the origin.
        </p>
      </statement>
      <proof>
        <p>
          If either vector is zero, then both sides are <m>0</m>, so the inequality is true.
        </p>
        <p>
          Now suppose both vectors are nonzero. Let <m>\theta</m> be the angle between them.
          Using the angle formula,
          <md>\mathbf u\cdot\mathbf v=\|\mathbf u\|\,\|\mathbf v\|\cos\theta.</md>
          Taking absolute values,
          <md>|\mathbf u\cdot\mathbf v|=\|\mathbf u\|\,\|\mathbf v\|\,|\cos\theta|.</md>
          Since
          <md>|\cos\theta|\leq 1,</md>
          we get
          <md>|\mathbf u\cdot\mathbf v|\leq \|\mathbf u\|\,\|\mathbf v\|.</md>
          Equality holds when
          <md>|\cos\theta|=1,</md>
          which means
          <md>\theta=0\quad\text{or}\quad \theta=\pi.</md>
          So the vectors point in the same direction or in opposite directions. Either way, they
          lie on the same line through the origin.
        </p>
      </proof>
    </theorem>

    <p>
      The equality condition needs the nonzero assumption. If
      <md>\mathbf u=\mathbf 0,</md>
      then
      <md>|\mathbf u\cdot\mathbf v|=0=\|\mathbf u\|\,\|\mathbf v\|</md>
      for every <m>\mathbf v</m>. Equality holds, but not because the zero vector has the same
      or opposite direction as <m>\mathbf v</m>. The zero vector has no direction.
    </p>

    <p>
      Cauchy–Schwarz says that the dot product cannot be too large. It is the algebraic
      version of a geometric fact: the shadow of a vector cannot exceed the vector itself.
    </p>

    <p>
      It also explains why the angle formula is safe:
      <md>\cos\theta=\frac{\mathbf u\cdot\mathbf v}{\|\mathbf u\|\|\mathbf v\|}.</md>
      Cauchy–Schwarz guarantees that the right side lies between <m>-1</m> and <m>1</m>, exactly
      where a cosine should live.
    </p>

    <p>
      The dot product measures agreement, projection, and work. But the quaternion
      product had another half. The vector part does not measure agreement; it measures
      oriented area. It points perpendicular to the plane of two vectors, and its length is the
      area of their parallelogram. That object is the cross product, and it is stranger than
      the dot product in one important way: it only works in three-dimensional space.
    </p>
  </subsection>
</section>
