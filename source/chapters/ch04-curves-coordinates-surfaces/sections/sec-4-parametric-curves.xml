<?xml version="1.0" encoding="UTF-8"?>
<!-- Converted from multivariable_calculus/chapters/ch04-curves-coordinates-surfaces.tex, Section 4.1.
     Status: CONVERTED. -->
<section xml:id="sec-4-parametric-curves" xmlns:xi="http://www.w3.org/2001/XInclude">
  <title>Parametric curves</title>

  <p>
    A graph like
    <md>y=f(x)</md>
    describes a curve by using <m>x</m> as the input. That works well for many curves, but it
    is not how a moving object usually reports its position. A boat, a drone, a planet, and a
    particle do not usually say, <q>here is my <m>y</m>-coordinate as a function of my
    <m>x</m>-coordinate.</q> They say where they are at each time.
  </p>

  <p>That is the idea of a parametric curve.</p>

  <subsection xml:id="c4s1-subsec-moving-points">
    <title>Curves as moving points</title>

    <p>
      Imagine an ant walking around the rim of a circular fountain of radius <m>3</m>. Put the
      center of the fountain at the origin. At time <m>t</m>, measured in radians, suppose the
      ant is at
      <md>\mathbf r(t)=(3\cos t,3\sin t).</md>
      A few times give:
    </p>

    <table xml:id="c4s1-tab-circle-values">
      <title>Values of <m>\mathbf r(t)</m> at selected times.</title>
      <tabular>
        <row header="yes" bottom="medium">
          <cell><m>t</m></cell>
          <cell><m>\mathbf r(t)</m></cell>
        </row>
        <row><cell><m>0</m></cell><cell><m>(3,0)</m></cell></row>
        <row><cell><m>\dfrac{\pi}{2}</m></cell><cell><m>(0,3)</m></cell></row>
        <row><cell><m>\pi</m></cell><cell><m>(-3,0)</m></cell></row>
        <row><cell><m>\dfrac{3\pi}{2}</m></cell><cell><m>(0,-3)</m></cell></row>
        <row><cell><m>2\pi</m></cell><cell><m>(3,0)</m></cell></row>
      </tabular>
    </table>

    <p>The point moves counterclockwise and returns to where it started.</p>

    <figure xml:id="c4s1-fig-circle-parametrization">
      <caption>A parametrization of a circle records a moving point, not just a set of points.</caption>
      <image width="60%">
        <latex-image>
        \begin{tikzpicture}[scale=0.8, &gt;=stealth]
            \draw[-&gt;] (-4,0) -- (4.4,0) node[right] {\(x\)};
            \draw[-&gt;] (0,-4) -- (0,4.4) node[above] {\(y\)};

            \draw[gray!25, step=1] (-3.5,-3.5) grid (3.5,3.5);

            \draw[thick, domain=0:6.283, samples=160, smooth, variable=\t]
                plot ({3*cos(\t r)}, {3*sin(\t r)});

            \draw[-&gt;, very thick] (3,0) arc[start angle=0,end angle=55,radius=3];

            \fill (3,0) circle (2pt);
            \fill (0,3) circle (2pt);
            \fill (-3,0) circle (2pt);
            \fill (0,-3) circle (2pt);

            \node[below right] at (3,0) {\(t=0\)};
            \node[above right] at (0,3) {\(t=\pi/2\)};
            \node[below left] at (-3,0) {\(t=\pi\)};
            \node[below right] at (0,-3) {\(t=3\pi/2\)};

            \node at (0,-4.7) {\(\mathbf r(t)=(3\cos t,3\sin t)\)};
        \end{tikzpicture}
        </latex-image>
      </image>
    </figure>

    <p>
      The coordinates satisfy
      <md>x=3\cos t,\qquad y=3\sin t.</md>
      Using
      <md>\cos^2t+\sin^2t=1,</md>
      we get
      <md>x^2+y^2=9.</md>
      So the image is the circle of radius <m>3</m>. But the parametrization gives more
      information than the equation <m>x^2+y^2=9</m>. It tells us where the ant starts, which
      direction it walks, and how the point is traced.
    </p>

    <definition xml:id="c4s1-def-parametric-curve">
      <title>Parametric curve</title>
      <statement>
        <p>
          A <term>parametric curve</term> in <m>\mathbb R^n</m> is a function
          <md>\mathbf r:I\to\mathbb R^n,</md>
          where <m>I</m> is an interval of real numbers. The input is called the
          <term>parameter</term>. Usually we write it as <m>t</m>.
        </p>
        <p>
          In the plane,
          <md>\mathbf r(t)=(x(t),y(t)).</md>
          In space,
          <md>\mathbf r(t)=(x(t),y(t),z(t)).</md>
        </p>
        <p>
          The <term>image</term> of the curve is the set of points
          <md>\{\mathbf r(t):t\in I\}.</md>
        </p>
      </statement>
    </definition>

    <p>
      The parameter often represents time, but it does not have to. It might represent an angle,
      a distance along a track, or simply a label that tells us where we are on the curve.
    </p>

    <p>
      There is one quiet condition we usually want: the coordinate functions should be
      continuous. A curve should not teleport.
    </p>

    <example xml:id="c4s1-ex-continuity-matters">
      <title>Why continuity matters</title>
      <statement>
        <p>
          Define
          <md>
            \mathbf r(t)=
            \begin{cases}
            (0,0), &amp; t&lt;0,\\
            (1,0), &amp; t\ge 0.
            \end{cases}
          </md>
          This rule gives a point for every <m>t</m>, but it does not describe a continuous
          motion. At <m>t=0</m>, the point jumps from <m>(0,0)</m> to <m>(1,0)</m>.
        </p>
        <p>
          So when we speak of a curve as a moving point, we usually mean that the component
          functions are continuous. Later, when we want velocity, we will ask for
          differentiability as well.
        </p>
      </statement>
    </example>
  </subsection>

  <subsection xml:id="c4s1-subsec-plane-space-curves">
    <title>Plane curves and space curves</title>

    <p>A plane curve has two coordinate functions. A space curve has three.</p>

    <example xml:id="c4s1-ex-line-segment">
      <title>A line segment as a parametric curve</title>
      <statement>
        <p>
          The straight path from
          <md>P=(1,2)</md>
          to
          <md>Q=(5,4)</md>
          can be written as
          <md>\mathbf r(t)=P+t(Q-P),\qquad 0\le t\le1.</md>
          Since
          <md>Q-P=(4,2),</md>
          this becomes
          <md>\mathbf r(t)=(1,2)+t(4,2)=(1+4t,2+2t).</md>
          At <m>t=0</m>, the curve is at <m>P</m>. At <m>t=1</m>, it is at <m>Q</m>. At
          <m>t=1/2</m>, it is halfway between them:
          <md>\mathbf r(1/2)=(3,3).</md>
        </p>
      </statement>
    </example>

    <p>
      This formula is the affine line from Chapter 1 with a restricted range of <m>t</m>. If we
      let <m>t</m> run through all real numbers, we get the whole line. If we keep
      <m>0\le t\le1</m>, we get only the segment.
    </p>

    <example xml:id="c4s1-ex-helix">
      <title>A helix around a spiral staircase</title>
      <statement>
        <p>
          A point moving around a circular staircase may have position
          <md>
            \mathbf r(t)=
            \left(
                2\cos t,\,
                2\sin t,\,
                \frac{t}{2\pi}
            \right),
            \qquad 0\le t\le 6\pi.
          </md>
          The <m>x</m>- and <m>y</m>-coordinates move around a circle of radius <m>2</m>. The
          <m>z</m>-coordinate rises steadily. One full turn raises the point by
          <md>\frac{2\pi}{2\pi}=1.</md>
          The image is a helix.
        </p>

        <!-- 3D-FIGURE: candidate for interactive Asymptote upgrade -->
        <figure xml:id="c4s1-fig-helix">
          <caption>A helix can be viewed as circular motion together with steady upward motion.</caption>
          <image width="70%">
            <latex-image>
            \begin{tikzpicture}[&gt;=latex]

                \begin{axis}[
                    name=helixplot,
                    view={35}{25},
                    width=8.5cm, height=8.5cm,
                    axis lines=center,
                    axis line style={gray, thin},
                    xlabel={\(x\)}, ylabel={\(y\)}, zlabel={\(z\)},
                    ticks=none,
                    xmin=-3.2, xmax=3.5,
                    ymin=-3.2, ymax=3.5,
                    zmin=0, zmax=3.8,
                ]

                    % Bounding cylinder base for depth perception
                    \addplot3[
                        gray!40, dashed,
                        domain=0:360,
                        samples=60,
                        samples y=0,
                        variable=\t
                    ] ({2*cos(\t)}, {2*sin(\t)}, 0);

                    % Vertical bounding lines
                    \draw[gray!40, dashed] (2,0,0) -- (2,0,3);
                    \draw[gray!40, dashed] (-2,0,0) -- (-2,0,3);

                    % 3D Helix
                    % 3 full turns = 1080 degrees. z = t/(2pi) -> t/360.
                    \addplot3[
                        thick, blue!50!black,
                        domain=0:1080,
                        samples=250,
                        samples y=0,
                        variable=\t,
                        smooth
                    ] ({2*cos(\t)}, {2*sin(\t)}, {\t/360});

                    % Displacement vector from t=0 to t=0.6 radians
                    \draw[-&gt;, very thick] (2,0,0) -- ({2*cos(deg(0.6))}, {2*sin(deg(0.6))}, {0.6/(2*pi)});

                \end{axis}

                % Label positioned neatly below the axis environment
                \node[align=center] at ([yshift=-0.5cm]helixplot.south) {a three-dimensional drawing of a space helix};

            \end{tikzpicture}
            </latex-image>
          </image>
        </figure>
      </statement>
    </example>

    <p>
      The helix is not the graph of a single function <m>y=f(x)</m>. It can pass over the same
      <m>x</m>-value many times. Parametric equations are more flexible than ordinary graph
      equations because each coordinate is allowed to depend on the parameter.
    </p>
  </subsection>

  <subsection xml:id="c4s1-subsec-eliminating-parameter">
    <title>Eliminating the parameter when possible</title>

    <p>Sometimes a parametric curve can be rewritten as a familiar Cartesian equation.</p>

    <example xml:id="c4s1-ex-parabola">
      <title>A parabola from a moving point</title>
      <statement>
        <p>
          Let
          <md>\mathbf r(t)=(t,t^2),\qquad -2\le t\le2.</md>
          Then
          <md>x=t,\qquad y=t^2.</md>
          Since <m>x=t</m>, we get
          <md>y=x^2.</md>
          So this parametrization traces the part of the parabola <m>y=x^2</m> with
          <md>-2\le x\le2.</md>
        </p>
      </statement>
    </example>

    <p>The parameter was easy to eliminate because one coordinate was just <m>t</m>.</p>

    <example xml:id="c4s1-ex-circle-trig">
      <title>A circle from trigonometry</title>
      <statement>
        <p>
          For
          <md>\mathbf r(t)=(3\cos t,3\sin t),</md>
          we have
          <md>x=3\cos t,\qquad y=3\sin t.</md>
          Squaring and adding gives
          <md>x^2+y^2=9\cos^2t+9\sin^2t=9.</md>
          So the image lies on
          <md>x^2+y^2=9.</md>
          If <m>0\le t\le2\pi</m>, every point on this circle is reached.
        </p>
      </statement>
    </example>

    <p>
      Eliminating the parameter can help us recognize the shape. But it can also hide important
      information.
    </p>

    <example xml:id="c4s1-ex-elimination-loses-info">
      <title>Elimination can lose information</title>
      <statement>
        <p>
          Consider
          <md>\mathbf r(t)=(t^2,t^4),\qquad -2\le t\le2.</md>
          Then
          <md>x=t^2,\qquad y=t^4.</md>
          Since
          <md>y=(t^2)^2,</md>
          we get
          <md>y=x^2.</md>
          But this parametrization does <em>not</em> trace the whole parabola <m>y=x^2</m>.
          Because
          <md>x=t^2\ge0,</md>
          it only traces the right-hand half:
          <md>y=x^2,\qquad 0\le x\le4.</md>
          Also, most points are traced twice: once by <m>t</m> and once by <m>-t</m>.
        </p>
      </statement>
    </example>

    <p>
      The Cartesian equation tells us the set of points. The parametrization tells us how the
      set is traveled.
    </p>
  </subsection>

  <subsection xml:id="c4s1-subsec-different-parametrizations">
    <title>Different parametrizations of the same curve</title>

    <p>The same geometric curve may have many different parametrizations.</p>

    <example xml:id="c4s1-ex-same-circle-clocks">
      <title>Same circle, different clocks</title>
      <statement>
        <p>
          The two parametrizations
          <md>\mathbf r_1(t)=(\cos t,\sin t),\qquad 0\le t\le2\pi,</md>
          and
          <md>\mathbf r_2(s)=(\cos 2s,\sin 2s),\qquad 0\le s\le\pi,</md>
          trace the same unit circle once in the counterclockwise direction.
        </p>
        <p>
          The second curve uses a different clock. When <m>s</m> increases by <m>\pi</m>, the
          angle <m>2s</m> increases by <m>2\pi</m>, so the point completes one full turn.
        </p>
      </statement>
    </example>

    <example xml:id="c4s1-ex-same-circle-opposite">
      <title>Same circle, opposite direction</title>
      <statement>
        <p>
          The parametrization
          <md>\mathbf r_3(t)=(\cos t,-\sin t),\qquad 0\le t\le2\pi,</md>
          also traces the unit circle. But it moves clockwise.
        </p>
        <p>
          At <m>t=0</m>,
          <md>\mathbf r_3(0)=(1,0).</md>
          At <m>t=\pi/2</m>,
          <md>\mathbf r_3(\pi/2)=(0,-1).</md>
          So the point goes downward first, not upward.
        </p>
      </statement>
    </example>

    <definition xml:id="c4s1-def-orientation">
      <title>Orientation of a parametrized curve</title>
      <statement>
        <p>
          The direction in which a parametrized curve is traced as the parameter increases is
          called its <term>orientation</term>.
        </p>
      </statement>
    </definition>

    <p>
      Orientation will matter later. If we integrate a <m>1</m>-form along a curve, reversing the
      curve reverses the sign of the integral. This is the one-dimensional version of the
      orientation signs we saw in determinants and wedge products:
      <md>dx\wedge dy=-dy\wedge dx.</md>
    </p>

    <definition xml:id="c4s1-def-closed-simple">
      <title>Closed and simple curves</title>
      <statement>
        <p>
          A parametrized curve
          <md>\mathbf r:[a,b]\to\mathbb R^n</md>
          is called <term>closed</term> if
          <md>\mathbf r(a)=\mathbf r(b).</md>
        </p>
        <p>
          It is called <term>simple</term> if it does not pass through the same point twice,
          except possibly at the endpoints of a closed curve.
        </p>
      </statement>
    </definition>

    <p>
      A circle traced once is closed and simple. A figure-eight curve is closed but not simple,
      because it crosses itself.
    </p>

    <example xml:id="c4s1-ex-self-crossing">
      <title>A curve with a self-crossing</title>
      <statement>
        <p>
          Let
          <md>\mathbf r(t)=(\sin t,\sin 2t),\qquad 0\le t\le2\pi.</md>
          At
          <md>t=0,\quad t=\pi,\quad t=2\pi,</md>
          the point is
          <md>(0,0).</md>
          The curve returns to the origin in the middle of its trip, so it has a self-crossing.
        </p>
      </statement>
    </example>

    <p>
      A curve is therefore more than its equation and more than its picture. It is a moving point
      with a chosen parameter and a chosen direction.
    </p>

    <p>
      Rectangular coordinates describe many curves well, but they are clumsy for curves that
      circle around a point. The circle
      <md>x^2+y^2=9</md>
      looks simpler as <q>distance from the origin equals <m>3</m>.</q> That sentence is not
      written in <m>x</m> and <m>y</m>. It is written in distance and angle. The next coordinate
      system is built for exactly that language.
    </p>
  </subsection>
</section>
